# 「活点地图」产品需求文档（PRD）

> 版本：v2.3（2026-08-12 产品定位修正：仿生式共享记忆、项目推进式协作、全图联想与画布内自动连接）
> 产品形态：本地优先、Agent 原生、仿生式共享记忆画布
> 发布方式：免费、开源，优先满足个人与小团队内部使用

---

## 一、产品概述

### 1.1 产品定义

活点地图是最适合开放式任务探索的人机协作工具。

它以仿生式共享记忆架构组织项目：人和 Agent 共用一份由地图索引与本地 Markdown 组成的项目推进式记忆。少量节点、路线、方案线、里程碑和标注表达探索结构，详细背景、尝试过程、证据和结论保存在 Markdown 中。

人负责提供业务上下文、目标、方案与审核判断；Agent 从合适的项目节点继续探索，检索全图相关因素形成联想，并把新方案、结果和证据写回同一份记忆。产品不是传统的问答 RAG 或知识库，而是帮助人和 Agent 持续共同推进开放式任务。

产品不是为了让人长时间在画布上写内容，而是让人快速看清：

- 项目正在向哪里推进。
- 哪些方案已经走通。
- 哪些方案已经失败。
- 哪些方案仍待验证。
- 当前出现了哪些值得单独解决的问题。

### 1.2 核心目标

创建一个可以同时承载多条探索路线、又能保持极度简洁的“活”地图。

核心目标：

1. 人可以一眼理解项目的探索状态。
2. 人与 Agent 共享同一份可检查、可修改、可恢复的项目记忆。
3. Agent 每次探索都能从合适节点延伸方案，而不是脱离项目状态重新回答。
4. 人和 Agent 能完成同一组地图操作；人的新标注在下次 Agent 会话中优先送达并得到明确确认。
5. 里程碑把关键判断交还给人审核，Agent 只能提出结论和证据。
6. Agent 能按当前问题检索全图、路线邻域与 Markdown，解释关联理由后给出方案。
7. 地图与本地项目文件共同存在，不依赖云端服务；复杂细节进入 Markdown，画布不承担文档编辑器职责。

### 1.3 产品原则

- 人看极简，Agent 读全量。
- 节点表达目的，方案线表达尝试。
- 多条探索路线可以同时存在。
- 方案状态属于方案线；节点另有稳定语义 `kind: goal|problem|result`。`problem` 表示尚未解决的问题，不等于失败方案。
- 文件是长期资产，界面只是查看与操作方式。
- Agent 是默认协作者，不是附加聊天功能。

---

## 二、目标用户与使用场景

### 2.1 目标用户

- 已经使用 WorkBuddy、Codex、Claude Code、Kimi、Cursor、Gemini 等 Agent 的个人用户。
- 需要与 Agent 共同研究、运营、排错、写作、设计或做产品的个人与小团队。
- 不希望维护复杂项目管理系统，但需要保留探索上下文的人。
- 希望所有资料留在本地项目目录中的用户。

### 2.2 典型场景

#### 场景 A：从零推进一个项目

用户从“做出一个动态壁纸”开始，随着理解加深，逐渐定义出生成图片、完成放大、生成动画等节点，并让 Agent 补充中间路线和方案。

#### 场景 B：为项目中的问题建立独立路线

用户在制作动态壁纸时发现“幽灵脸问题”，在主路线旁建立一条独立问题路线。该路线可以包含多个方案，最终以“幽灵脸问题解决”结束。

#### 场景 C：并行探索多个问题

同一画布同时存在项目主路线、幽灵脸路线、性能路线和输出格式路线。每条路线可以独立推进，不必挤在单一流程图上。

#### 场景 D：Agent 自动维护地图

用户在已经使用的 Agent 中描述任务。活点地图协作层优先注入人的新标注，再根据当前问题检索相关节点、路线与 Markdown；Agent 从合适节点继续探索，新增节点、创建方案、更新状态并记录证据。用户重新查看画布时能直接看到变化。

---

## 三、核心价值与差异化

### 3.1 地图，不是文档容器

Obsidian Canvas、Miro 和通用白板倾向于把内容本身放进画布。活点地图只在画布上保留结构和状态，内容继续留在 Markdown 中。

### 3.2 探索，不是任务管理

产品不要求截止日期、负责人、优先级、工时和固定流程。探索允许失败、回退、悬空和出现新的问题路线。

### 3.3 Agent 原生，不是 AI 按钮

Agent 不只是生成一段文字，而是与人共同操作地图。Agent 能创建节点、调整布局、建立路线、更新方案状态、写入标注并维护 Markdown。

### 3.4 共享推进式记忆，不是传统 RAG

活点地图不只按关键词返回若干文档片段。它同时保留目标、路线、方案、结果、失败、里程碑、人类新标注及其关系；Agent 按当前节点和问题检索全图相关因素，说明为什么产生关联，并从正确位置继续探索。人看到的策展图与 Agent 使用的项目记忆是同一份事实源，不存在人无法审核的私有记忆层。

### 3.5 本地优先，不绑定 Agent 供应商

地图数据和 Markdown 位于本地项目目录。不同 Agent 只要理解同一套 skill/文件规范，就可以操作地图，不要求产品内置特定模型或云账号。

### 3.6 轻量、免费、开源

第一目标是个人和团队真正愿意每天打开使用，而不是建立商业化协作平台。项目计划免费开源，数据格式应开放、可迁移。

---

## 四、产品术语与核心模型

### 4.1 探索地图

一个项目可以有多张探索地图，每张地图是一个任务的独立记忆（2026-08-16 起，原为「一个项目一张地图」）。地图包含多条路线、节点、方案线和标注，并保存画布布局信息。

### 4.2 路线

路线是一组具有独立探索上下文的节点和方案线。

路线可以是：

- 项目主路线，例如“动态壁纸”。
- 问题路线，例如“幽灵脸问题”。
- 专题路线，例如“性能优化”或“输出格式”。

一张地图可以同时存在任意数量的路线。

问题路线可以记录它来源于哪个节点，但默认不需要在画布上绘制一条关系线，以免破坏简洁性。

### 4.3 节点

节点表示一个需要被理解的人类审核点，可以是：

- 目的：想达到什么。
- 问题：发现了什么阻碍。
- 结果：已经达成什么。

节点不是具体方案，也不等同于任务。

节点应包含：

- 唯一 ID。
- 短名称。
- `kind`（目标、问题、结果）；旧 `type` 字段仅作兼容显示。
- 所属路线。
- 画布位置与尺寸。
- 关联 Markdown 文件。
- 人类标注。
- 创建与更新时间。

### 4.4 方案线

方案线是从一个节点出发的一次具体尝试，例如：

- 修改提示词。
- 更换模型。
- 更换工作流。
- 引入遮罩检测。

方案名称附着在线上，不创建成节点。

方案线应包含：

- 唯一 ID。
- 起点节点。
- 可为空的目标节点。
- 方案名称。
- 状态。
- 所属路线。
- 线条几何信息。
- 关联 Markdown 文件。
- 人类标注。
- 创建与更新时间。

### 4.5 标注

标注是人写给 Agent 或未来自己的简短信息，可以附着在节点、方案线或画布位置上。

标注可以隐藏，但隐藏后仍然属于地图数据，Agent 必须能够读取。

---

## 五、方案状态与连接规则

### 5.1 成功方案

- 使用绿色实线表达。
- 以明确箭头表达方向。
- 通常连接起点和目标节点，表示该方案已经验证，并推进到新的目的或结果。
- 连接不是成功的强制条件：绿色线也允许悬空（状态与连接解耦，见 §5.4）。

### 5.2 失败方案

- 使用红色虚线表达。
- 通常只有起点、末端悬空，表示该方案已经被验证为无效。
- 允许连接目标节点：是否连接由人决定，与状态无关（见 §5.4）。

### 5.3 待验证方案

- 使用灰色点线表达。
- 通常只有起点、末端悬空，表示一个尚未完成验证的探索方向。
- 允许连接目标节点：是否连接由人决定，与状态无关（见 §5.4）。

### 5.4 状态与连接的关系（走查 5/6/10 修订）

- **状态与连接解耦**：绿/红/灰三种状态均可连接或不连接节点。
- **手动修改状态只改变线条颜色与线型，不改变连接关系**（走查 10）。
- 连接操作附带默认状态推断（走查 5/6 确认的原型行为）：拖拽端点吸附到节点 → 默认标为成功；拖起脱钩悬空 → 默认回到待验证。推断结果可由人再手动修改，手动修改不动连接。
- 新建方案线默认标记为「待验证」；右键「新建连接节点」在悬空端建节点并连接，线保持原状态。
- 人与 Agent 修改状态或连接时，遵守相同规则。
- 节点不使用成功、失败、待验证三种状态色。

---

## 六、多路线探索规则

### 6.1 路线并行

- 多条路线在同一无限画布中自由排列。
- 每条路线拥有自己的节点顺序、方案和状态。
- 一条路线的失败或待验证方案不影响其他路线。
- 用户可以移动整条路线，也可以单独移动节点。

### 6.2 从问题创建路线（走查 8 修订）

用户或 Agent 可以从任意节点创建问题路线；节点是否为 `kind:problem` 与是否创建路线是两个独立决定。

创建时（就地升级）：

1. 若节点尚未标为问题，先将其 `kind` 设为 `problem`（保留原节点，不把路线当作问题本身）。
2. 新建一条以其命名的「分支路线N」，记录问题路线与来源节点的关系。
3. 该节点直连的方案线与对端节点一并归入新路线；之后该节点新连接的节点、方案都属于这个路线。
4. 已是路线起点的节点不重复升级。
5. 后续由人或 Agent 逐渐补充方案和结果节点。

### 6.3 路线结束

路线不要求固定长度。问题路线通常在问题解决节点结束；项目主路线可以持续扩展。

---

## 七、核心使用流程

### 7.1 首次进入（走查 1/5 修订）

1. 用户打开活点地图，直接进入无限画布，无独立首屏页、无中央输入态。
2. 新建地图直接进入空白画布；打开、新建、导入、保存/导出收进画布左上角的项目菜单。
3. 用户在画布点击“启用 Agent 协作”；本地桥自动发现已经安装的受支持 Agent，不要求用户先选择 Agent。
4. 已存在本地桥时自动连接当前项目；尚未安装时，画布提供一句自然语言接入指令，由用户已有的 Agent 自主完成安装、授权、自检和回连，用户不接触命令行。
5. 用户可以让已有 Agent 根据当前项目生成起始地图草案，也可以手动新建第一个节点。
6. Agent 生成草案时遵守地图简洁协议，先呈现少量关键节点、路线和里程碑，不把项目文件目录直接变成节点堆。

### 7.2 人工编辑

用户可以：

- 新建、重命名、移动和缩放节点。
- 新建问题路线。
- 从节点创建方案线。
- 修改方案名称和状态。
- 为节点和方案线添加标注。
- 显示或隐藏标注。
- 打开关联 Markdown。
- 删除或恢复地图对象。
- 调整路线在画布上的位置。

### 7.3 Agent 编辑

Agent 可以完成与人相同的地图操作：

- 根据项目内容生成节点和路线。
- 根据对话创建问题路线。
- 补充中间节点和方案。
- 更新方案状态。
- 写入或更新 Markdown。
- 调整画布布局，避免节点重叠。
- 在新会话中优先读取人的新标注，明确引用标注 ID，并在服务端确认后更新已读状态。
- 根据当前问题检索全图相关节点、路线和 Markdown，解释关联理由，从合适节点继续延伸方案。
- 提交里程碑结论与证据，可以写入自身结论状态（包括 `approved`），但不能伪造人类创建或人类确认；来源、更新者和证据必须在画布上可区分。

### 7.4 人类审核

Agent 更新后，用户主要通过画布审核：

- 新增了什么节点或路线。
- 哪些方案变绿、变红或仍为灰色。
- 当前问题是否已经形成结果节点。
- Agent 是否正确理解了人的标注。
- 待审核里程碑是否满足标准，以及是否批准继续推进。

---

## 八、功能需求

### 8.1 无限画布

- 支持缩放、平移、框选和自动适配视图。
- 支持节点自由移动和缩放。
- 支持多条路线同时显示。
- 支持大量节点时保持清晰和流畅。

### 8.2 节点操作（走查 3/7/8 修订）

- 新建、编辑、复制和删除节点。
- 节点保持圆形。
- 节点不显示编号；短名称直接写在圆内，圆随文字自动适配，长名称自动换行。
- 内部编号仍存在于数据中，仅在属性面板底部以可隐藏的小字显示，用于文件命名与 Agent 引用。
- 节点 `kind` 为 goal / problem / result 三类；问题节点用红色语义徽标与浅红底可一眼识别，可另外从问题建立路线；面板保留自定义显示类型。
- 节点可在画布内打开、编辑和保存关联 Markdown；正式协作通过本地桥读写并处理并发冲突。

### 8.3 方案线操作（走查 2/5/6/9/10 修订）

- 从节点创建方案线：在节点上按住直接拉出，新线默认「待验证」（灰点线，是否连接不影响状态）；拉完一条可连续拉下一条。
- 编辑方案名称（画布标签双击与面板行内编辑）。
- 切换成功、失败、待验证状态，只改颜色与线型，不改变连接（见 §5.4）。
- 所有方案线的端点均可拖拽：命中节点吸附连接（默认标成功），拖起脱钩悬空（默认回待验证）；之后可手动改状态，手动改状态不动连接（见 §5.4）。
- 悬空线可通过右键「新建连接节点」在端点自动建「结果」类型节点并连接，线保持原状态。
- 线身可按住弯折调整；连到同一目标的多条线自动错开。
- 支持一键隐藏全部失败方案线（数据保留，不参与框选）。
- 方案名称在隐藏标注时仍然显示。

### 8.4 路线操作（走查 8/11 修订）

- 新建、命名和删除路线。
- 普通节点就地升级为问题路线起点（见 §6.2）。
- 记录路线来源。
- 节点与方案线的「所属路线」可在属性面板下拉改挂到任意路线或「无」；新建节点默认「无」，新建方案线跟随起点节点所属路线。
- 路线名称在画布上低强调显示，可双击改名，可全局隐藏。
- 支持整条路线移动或自动整理。
- 支持按路线聚焦查看。

### 8.5 标注操作

- 在节点、方案线或画布位置创建标注。
- 编辑和删除标注。
- 全局显示/隐藏标注。
- 单独显示/隐藏标注。
- Agent 始终能够读取隐藏标注。

### 8.6 属性与 Markdown

- 选择对象后显示对应属性。
- 节点和方案线都可以关联 Markdown。
- 可以从地图直接打开对应文件。
- Markdown 变化不会自动把长内容展开到画布。

### 8.7 导入、导出与恢复

- 支持打开现有项目地图。
- 支持导出开放格式备份。
- 支持从备份恢复。
- 支持画布连续操作的撤销和重做；撤销结果仍通过本地桥可靠保存。
- 人和 Agent 的已保存操作都记录 revision、操作者与变更对象，可在协作历史中检查。
- 撤销已保存的 Agent 或人类修改时，以冲突安全的反向命令生成新 revision，不删除原历史，也不能静默覆盖其后的其他修改。
- Agent 修改后通过事件流同步到画布；本地有未保存草稿时不得直接覆盖，必须进入冲突处理。

---

## 九、Agent 协作需求

### 9.1 文件优先

Agent 协作的核心不是网页内聊天，而是项目文件。

项目目录对人保持简单：一个 map.json 索引 + Markdown 分片。map.json 保存地图的结构、状态、布局与 Markdown 链接，是人机共读的单一事实源。正式协作由本地桥按需生成全局推进摘要、人的未读标注和与当前问题相关的检索上下文；长文本不进入 map.json，详情一律落在 Markdown。

当前结构（v2，详细 schema 见 `docs/map-json-v2.md`；v1 仅用于迁移兼容）：

```text
项目目录/
├── .live-dot-map/
│   ├── map.json              # 地图索引：路线/节点/方案线/标注的结构、状态与布局
│   ├── nodes/                # 节点详情 Markdown
│   └── routes/               # 路线与方案记录 Markdown
└── AGENTS.md                 # 项目级路由与长期协作规则
```

画布应用是独立的单文件 HTML，可打开任意项目文件夹；项目目录不需要存放应用本体。

### 9.2 自动连接与协作适配

产品不要求用户在进入画布前选择 Agent。用户已经拥有并正在使用自己的 Agent；画布中的“启用 Agent 协作”负责发现和连接。

- 本地桥扫描 `PATH`、常见用户安装目录和显式配置，识别已安装的受支持 Agent CLI。
- 自动为当前项目连接所有能够可靠适配的 Agent；需要平台信任时，只呈现该平台自身的一次确认。
- 尚未安装本地桥时，提供一句给 Agent 的自然语言接入指令；命令由 Agent 自主执行，不暴露给普通用户。
- 连接失败必须给出可理解的原因、修复动作和实际创建的启动入口，不能静默降级。

官方 Skill、插件或项目适配层负责告诉 Agent：

- 活点地图的文件位置和结构。
- 节点、路线、方案、状态和标注的语义。
- 创建和更新地图的正确方式。
- 方案状态与连接解耦等语义规则（§5.4）。
- 如何在修改后保持数据完整。

`AGENTS.md` 和 Skill 是协作语义与降级安全网，不作为“新会话一定自动注入”的唯一保证。完整体验必须由平台生命周期能力或活点地图掌控的等价自动触发点完成，并由未读状态与确认记录验证结果。

### 9.3 正式本地桥接

本地桥是正式协同模式的统一入口：

- 可靠保存、冲突、备份、恢复和 Agent 写回统一经过本地桥。
- 自动发现当前项目和本机 Agent 状态，并完成项目级连接。
- 新会话优先送达人的未读标注；每轮按问题检索全图相关上下文。
- Agent 修改后画布实时刷新，并区分已保存、未读、已送达、已确认和未闭环。

纯文件模式仍可查看和迁移数据，但不能宣称自动注入、并发保护或完整协作正常。

### 9.4 Agent 安全边界

- Agent 默认只读写当前项目目录。
- 不把私密文件内容写入地图日志。
- 删除大量节点、路线或文件前需要人确认。
- 数据写入必须可验证、可恢复。

---

## 十、界面与体验需求

详细要求见：[UI设计需求文档.md](./UI设计需求文档.md)

### 10.1 界面方向（走查 1/5 修订）

- 打开即画布：无独立首屏页、无中央输入态；Agent 入口在右下角按需展开（原「Agent 首屏参考 ChatGPT Work / Codex」方向已被走查推翻）。
- 无限画布和基础操作参考 Excalidraw。
- 属性面板和菜单参考 Linear 的克制信息层级。
- 本地文件关系参考 Obsidian Canvas。

### 10.2 必须满足

- 禁止多余文字。
- 画布是界面主体。
- 不显示常驻 Agent 聊天窗口。
- 方案状态一眼可辨。
- 节点与方案职责清楚。
- 属性面板按需出现。
- 详细信息不进入画布。
- 用户不需要理解 CLI、MCP 和数据格式才能使用。

### 10.3 原型方式

- 后续使用可交互 HTML 原型审核 UI。
- 静态 SVG 只用于简单结构示意。
- UI 通过后再进入完整功能改造。

---

## 十一、技术与产品形态

### 11.1 自研轻量 Web 应用

画布以定稿原型 `canvas.html` 为基准自研演进。源码允许模块化 TypeScript；构建后仍交付单个 `app.html` 与一个无运行时 npm 依赖的 `livedot.mjs`。`app.html` 负责画布交互，轻量本地桥统一负责可靠存储、恢复、检索、Agent 连接和写回。

自有实现：

- 无限画布、缩放平移、选择拖拽、撤销重做与基础快捷键（原型已具备，继续完善）。
- 节点、路线、方案和标注语义。
- 三种方案状态及其与连接解耦的规则（§5.4）。
- map.json 读写、Markdown 分片与文件同步。
- Agent 协作协议。
- 项目管理和属性面板。

历史说明：阶段 2 曾基于 Excalidraw fork 复刻原型交互（已否决并存档，见 §16），fork 仅作交互参考实现保留。

### 11.2 许可与分发

产品本体为自研代码，计划免费开源。存档的 Excalidraw fork 仅作参考；如未来从其复制代码片段，须遵守其 MIT License 保留版权与许可声明，并使用活点地图自己的产品名称和品牌。

### 11.3 本地优先

- 默认可以离线查看和编辑。
- 不要求登录账号。
- 不要求云数据库。
- 项目文件可以使用 Git、网盘或团队已有工具同步。

### 11.4 发布形态

目标仍然是轻量本地应用：用户看到的是 `app.html` 画布，正式协同由轻量本地桥提供可靠文件事务、自动发现、Agent 连接和动态上下文注入。画布内提供“启用 Agent 协作”，不内置聊天界面，也不要求用户先选择 Agent。

直接双击 `app.html` 可进入纯浏览器降级模式，但必须明确提示自动注入、并发保护和可靠保存认证未启用。桌面壳只在浏览器与本地桥无法继续降低接入成本时评估，不作为当前前提。

### 11.5 性能

- 常见地图打开和操作应当即时响应。
- 支持至少百级节点和方案线流畅使用。
- Agent 更新时不阻塞画布浏览。
- 大量 Markdown 不应一次性渲染到画布。

---

## 十二、非目标

第一阶段不做：

- 通用白板和自由绘画软件。
- 富文本笔记编辑器。
- Jira/Linear 式项目管理系统。
- 产品内完整聊天客户端。
- 云端模型市场和模型供应商管理。
- 强制注册、登录和云同步。
- 复杂多人实时光标协作。
- 移动端完整编辑体验。

---

## 十三、典型案例

### 13.1 项目主路线

```text
开始
  ──生成基础图片──>
生成图片
  ──实现放大──>
完成放大
  ──生成动画──>
动态壁纸完成
```

绿色方案线连接每个已经确认的结果节点。

### 13.2 幽灵脸问题路线

```text
幽灵脸问题
  ├── 修改提示词       红色失败，悬空
  ├── 更换模型         灰色待验证，悬空
  └── 引入遮罩检测     绿色成功 ──> 问题解决
```

“修改提示词”“更换模型”“引入遮罩检测”都是方案线名称，不是节点。

该问题路线可以放在项目主路线旁边，并记录它来源于“生成图片”或“完成放大”节点。

---

## 十四、成功标准

### 14.1 人类使用

- 新用户无需教程即可创建第一张地图。
- 用户无需理解或手动输入命令行；已有 Agent 可以自主完成安装与当前项目连接。
- 用户可以同时理解至少三条并行路线。
- 用户可以一眼区分成功、失败和待验证方案。
- 画布在隐藏标注后仍保留完整路线语义。
- 用户无需打开长文档就能判断当前探索状态。
- 用户在画布创建或修改的标注可靠保存，并能看见 Agent 是否真正读取。
- 用户能通过里程碑审核关键结果，Agent 不能自行越过待审节点。

### 14.2 Agent 使用

- Agent 在受支持的新会话中优先收到全部人类未读标注，并明确引用标注 ID。
- Agent 可以创建节点、路线和方案。
- Agent 可以正确更新方案状态和目标关系。
- Agent 遵守状态与连接解耦规则：不把状态变更与连接变更互相强制联动（§5.4）。
- Agent 可以读取隐藏标注和对应 Markdown。
- Agent 能根据当前问题召回相关节点、路线和 Markdown，说明关联理由，并从合适节点继续探索。
- Agent 可以直接更新里程碑状态（包括 `approved`）；状态表示该 Agent 的结论，不等于人类确认，来源、创建者、更新者和证据必须可见。
- Agent 更新后，地图数据仍然完整并可打开。

### 14.3 产品质量

- 地图数据开放、可备份、可恢复。
- 本地使用不依赖云端服务。
- 常见操作响应流畅。
- 产品界面保持简洁，不随着功能增加变成仪表盘。

---

## 十五、开发优先级

### P0：核心可用

- 自研单文件 HTML 无限画布（canvas.html 原型演进为 app.html）。
- 圆形节点与自由布局。
- 多路线并行。
- 成功、失败、待验证三种方案线。
- 方案状态与连接解耦的规则（§5.4）。
- 方案名称和标注。
- 节点/方案关联 Markdown。
- 本地保存、打开、导入、导出。
- Agent 可读写的结构化文件规范。
- 官方 skill/`AGENTS.md` 指南。
- 打开即画布，画布内“启用 Agent 协作”与手动创建入口。

### P1：无感协作

- 外部文件变化自动刷新。
- 本地桥可靠保存、恢复、冲突保护与 Agent 写回。
- 自动发现本机 Agent，并在画布中一键启用当前项目协作。
- 人类新标注的新会话自动送达、引用确认与未读重投。
- 基于全图结构和 Markdown 的相关信息检索。
- Agent 更新对象高亮。
- 最近项目。
- 路线聚焦和自动整理。
- 搜索节点与方案。

### P2：团队增强

- Git 友好的变更与冲突处理。
- 地图快照和版本对比。
- 只读分享。
- 可选团队同步。

### 远期构想（已讨论，明确暂缓）

> 2026-08-08 阶段 6 规划时记录：以下方向讨论过但**主动暂缓**，避免在狗粮验证完成前摊大工程面。重启条件：壁纸项目狗粮跑顺 + 决定公开仓库之后，逐条重估。

- **画布数据端到端加密（E2E）**：公网托管的是纯静态文件，数据本就在用户本地，暂无加密对象；等做「分享协作」时再一并设计（加密与网页内嵌 AI 存在天然矛盾：E2E 意味着服务端读不到内容，AI 编辑画布就无处运行，二选一或做成分层开关）。
- **分享与协作**：只读分享链接、多人编辑，依赖后端，属于 P2 之后的产品形态决策，不在本地优先阶段做。
- **网页内嵌 AI（网页里直接和 AI 对话编辑画布）**：需要后端与模型接入，与「零依赖单文件、本地优先」红线冲突，暂缓。
- **官方插件商店分发**：Skill、插件和平台适配器只是连接层，不是产品本体。当前先用画布内“启用 Agent 协作”与一句自然语言接入兜底；真实闭环验收后再逐个平台上架，不能让商店审核阻塞产品使用。
- **自有域名**：现有 Cloudflare Workers 域名 + CloudBase 备用域名双点已够用；自有域名涉及备案与证书维护，暂缓。

---

## 十六、现有 Demo 与存档（截至 2026-08-06 的状态）

- ReactFlow 版本验证 Demo 已废弃，不再作为最终 UI 和画布技术基础，不再维护。
- 现有 SVG 状态稿只保留为讨论记录，不作为视觉验收标准。
- 可交互 HTML 原型已完成并经 11 轮走查定稿，冻结为验收样板（`canvas.html`）；正式产品从其复制出的 `app.html` 演进。
- 阶段 2 的 Excalidraw fork 复刻已完成并走查修复，但因底层数据格式需自研被否决（2026-08-06）：master 基线已备份至 GitHub 私有仓库 `hhh-dahah/live-dot-map-canvas`，全部改造工作保留在本地 `wip-stage2` 分支，仅作交互参考。
- 当前为 v2 可靠性候选验收阶段：正式协同统一经过本地桥，纯 HTML 只作降级模式；最新状态以 `goal.md` 与 `implement.md` 为准。

---

## 十七、产品总结

活点地图的核心不是画图，而是让人和 Agent 共享同一份项目推进式记忆。

人看到的是少量节点、路线、方案、里程碑和标注；Agent 使用的是同一份结构与 Markdown，并优先读取人的新信息。Agent 从合适节点继续探索，按当前问题联想全图相关因素；人提供业务上下文、目标和审核判断。项目可以不断产生新的问题路线，也可以保留失败尝试，而不会失去整体方向。

最终产品应当做到：

> 最适合开放式任务探索的人机协作工具。
