# 「活点地图」产品需求文档（PRD）

> 版本：v2.0  
> 产品形态：本地优先、Agent 原生、轻量无限画布  
> 发布方式：免费、开源，优先满足个人与小团队内部使用

---

## 一、产品概述

### 1.1 产品定义

活点地图是一个面向人机协作的探索状态画布。

它用少量节点、路线、方案线和标注表达一个项目的探索过程；详细背景、尝试过程和结论保存在本地 Markdown 文件中。人负责观察、判断和少量修改，Agent 可以读取同一组文件并持续更新地图。

产品不是为了让人长时间在画布上写内容，而是让人快速看清：

- 项目正在向哪里推进。
- 哪些方案已经走通。
- 哪些方案已经失败。
- 哪些方案仍待验证。
- 当前出现了哪些值得单独解决的问题。

### 1.2 核心目标

创建一个可以同时承载多条探索路线、又能保持极度简洁的“活”地图。

核心目标：

1. 人可以一眼理解项目的探索状态。
2. Agent 可以准确读取和修改地图结构。
3. 人和 Agent 能完成同一组地图操作。
4. 地图与本地项目文件共同存在，不依赖云端服务。
5. 复杂细节进入 Markdown，画布不承担文档编辑器职责。

### 1.3 产品原则

- 人看极简，Agent 读全量。
- 节点表达目的，方案线表达尝试。
- 多条探索路线可以同时存在。
- 状态属于方案线，不属于节点。
- 文件是长期资产，界面只是查看与操作方式。
- Agent 是默认协作者，不是附加聊天功能。

---

## 二、目标用户与使用场景

### 2.1 目标用户

- 使用 Codex、Claude Code、Cursor、Gemini CLI 等本地 Agent 的个人开发者。
- 需要与 Agent 共同研究、排错、写作或做产品的小团队。
- 不希望维护复杂项目管理系统，但需要保留探索上下文的人。
- 希望所有资料留在本地项目目录中的用户。

### 2.2 典型场景

#### 场景 A：从零推进一个项目

用户从“做出一个动态壁纸”开始，随着理解加深，逐渐定义出生成图片、完成放大、生成动画等节点，并让 Agent 补充中间路线和方案。

#### 场景 B：为项目中的问题建立独立路线

用户在制作动态壁纸时发现“幽灵脸问题”，在主路线旁建立一条独立问题路线。该路线可以包含多个方案，最终以“幽灵脸问题解决”结束。

#### 场景 C：并行探索多个问题

同一画布同时存在项目主路线、幽灵脸路线、性能路线和输出格式路线。每条路线可以独立推进，不必挤在单一流程图上。

#### 场景 D：Agent 自动维护地图

用户在本地 Agent 中描述任务。Agent 根据 skill 或 `AGENTS.md` 读取地图和 Markdown，新增节点、创建方案、更新状态并记录结论。用户重新查看画布时能直接看到变化。

---

## 三、核心价值与差异化

### 3.1 地图，不是文档容器

Obsidian Canvas、Miro 和通用白板倾向于把内容本身放进画布。活点地图只在画布上保留结构和状态，内容继续留在 Markdown 中。

### 3.2 探索，不是任务管理

产品不要求截止日期、负责人、优先级、工时和固定流程。探索允许失败、回退、悬空和出现新的问题路线。

### 3.3 Agent 原生，不是 AI 按钮

Agent 不只是生成一段文字，而是与人共同操作地图。Agent 能创建节点、调整布局、建立路线、更新方案状态、写入标注并维护 Markdown。

### 3.4 本地优先，不绑定 Agent 供应商

地图数据和 Markdown 位于本地项目目录。不同 Agent 只要理解同一套 skill/文件规范，就可以操作地图，不要求产品内置特定模型或云账号。

### 3.5 轻量、免费、开源

第一目标是个人和团队真正愿意每天打开使用，而不是建立商业化协作平台。项目计划免费开源，数据格式应开放、可迁移。

---

## 四、产品术语与核心模型

### 4.1 探索地图

一个项目对应一张探索地图。地图包含多条路线、节点、方案线和标注，并保存画布布局信息。

### 4.2 路线

路线是一组具有独立探索上下文的节点和方案线。

路线可以是：

- 项目主路线，例如“动态壁纸”。
- 问题路线，例如“幽灵脸问题”。
- 专题路线，例如“性能优化”或“输出格式”。

一张地图可以同时存在任意数量的路线。

问题路线可以记录它来源于哪个节点，但默认不需要在画布上绘制一条关系线，以免破坏简洁性。

### 4.3 节点

节点表示一个需要被理解的人类审核点，可以是：

- 目的：想达到什么。
- 问题：发现了什么阻碍。
- 结果：已经达成什么。

节点不是具体方案，也不等同于任务。

节点应包含：

- 唯一 ID。
- 短名称。
- 节点类型。
- 所属路线。
- 画布位置与尺寸。
- 关联 Markdown 文件。
- 人类标注。
- 创建与更新时间。

### 4.4 方案线

方案线是从一个节点出发的一次具体尝试，例如：

- 修改提示词。
- 更换模型。
- 更换工作流。
- 引入遮罩检测。

方案名称附着在线上，不创建成节点。

方案线应包含：

- 唯一 ID。
- 起点节点。
- 可为空的目标节点。
- 方案名称。
- 状态。
- 所属路线。
- 线条几何信息。
- 关联 Markdown 文件。
- 人类标注。
- 创建与更新时间。

### 4.5 标注

标注是人写给 Agent 或未来自己的简短信息，可以附着在节点、方案线或画布位置上。

标注可以隐藏，但隐藏后仍然属于地图数据，Agent 必须能够读取。

---

## 五、方案状态与强制规则

### 5.1 成功方案

- 使用绿色实线表达。
- 必须具有起点和目标节点。
- 必须以明确箭头连接目标节点。
- 表示该方案已经验证，并推进到新的目的或结果。

### 5.2 失败方案

- 使用红色虚线表达。
- 只有起点，没有目标节点。
- 末端必须悬空。
- 表示该方案已经被验证为无效。

### 5.3 待验证方案

- 使用灰色点线表达。
- 只有起点，没有目标节点。
- 末端必须悬空。
- 表示一个尚未完成验证的探索方向。

### 5.4 状态约束

- 失败和待验证方案禁止连接下一节点。
- 成功方案必须连接下一节点。
- 方案从待验证变为成功时，必须同时创建或选择目标节点。
- 方案从成功改为失败/待验证时，必须移除目标关系，但不删除原目标节点。
- 方案状态由人或 Agent 修改时，都必须遵守相同规则。
- 节点不使用成功、失败、待验证三种状态色。

---

## 六、多路线探索规则

### 6.1 路线并行

- 多条路线在同一无限画布中自由排列。
- 每条路线拥有自己的节点顺序、方案和状态。
- 一条路线的失败或待验证方案不影响其他路线。
- 用户可以移动整条路线，也可以单独移动节点。

### 6.2 从问题创建路线

用户或 Agent 可以从任意节点创建问题路线。

创建时：

1. 在原节点附近建立一个独立问题节点。
2. 记录问题路线与来源节点的关系。
3. 不自动创建中间节点和方案线。
4. 后续由人或 Agent 逐渐补充方案和结果节点。

### 6.3 路线结束

路线不要求固定长度。问题路线通常在问题解决节点结束；项目主路线可以持续扩展。

---

## 七、核心使用流程

### 7.1 首次进入

1. 用户打开活点地图。
2. 用户直接描述想做的事，或选择本地项目。
3. Agent 根据目标和项目内容创建起始地图。
4. 用户也可以不使用 Agent，手动新建第一个节点。
5. 地图建立后进入无限画布。

### 7.2 人工编辑

用户可以：

- 新建、重命名、移动和缩放节点。
- 新建问题路线。
- 从节点创建方案线。
- 修改方案名称和状态。
- 为节点和方案线添加标注。
- 显示或隐藏标注。
- 打开关联 Markdown。
- 删除或恢复地图对象。
- 调整路线在画布上的位置。

### 7.3 Agent 编辑

Agent 可以完成与人相同的地图操作：

- 根据项目内容生成节点和路线。
- 根据对话创建问题路线。
- 补充中间节点和方案。
- 更新方案状态。
- 写入或更新 Markdown。
- 调整画布布局，避免节点重叠。
- 发现人的新标注并在后续工作中使用。

### 7.4 人类审核

Agent 更新后，用户主要通过画布审核：

- 新增了什么节点或路线。
- 哪些方案变绿、变红或仍为灰色。
- 当前问题是否已经形成结果节点。
- Agent 是否正确理解了人的标注。

---

## 八、功能需求

### 8.1 无限画布

- 支持缩放、平移、框选和自动适配视图。
- 支持节点自由移动和缩放。
- 支持多条路线同时显示。
- 支持大量节点时保持清晰和流畅。

### 8.2 节点操作

- 新建、编辑、复制和删除节点。
- 节点保持圆形。
- 节点只显示编号和短名称。
- 节点可关联和打开 Markdown。

### 8.3 方案线操作

- 从节点创建方案线。
- 编辑方案名称。
- 切换成功、失败、待验证状态。
- 成功方案支持磁吸到目标节点。
- 失败和待验证方案保持悬空。
- 方案名称在隐藏标注时仍然显示。

### 8.4 路线操作

- 新建、命名和删除路线。
- 从节点创建问题路线。
- 记录路线来源。
- 支持整条路线移动或自动整理。
- 支持按路线聚焦查看。

### 8.5 标注操作

- 在节点、方案线或画布位置创建标注。
- 编辑和删除标注。
- 全局显示/隐藏标注。
- 单独显示/隐藏标注。
- Agent 始终能够读取隐藏标注。

### 8.6 属性与 Markdown

- 选择对象后显示对应属性。
- 节点和方案线都可以关联 Markdown。
- 可以从地图直接打开对应文件。
- Markdown 变化不会自动把长内容展开到画布。

### 8.7 导入、导出与恢复

- 支持打开现有项目地图。
- 支持导出开放格式备份。
- 支持从备份恢复。
- 支持撤销和重做。
- 外部 Agent 修改文件后，地图可以重新加载变更。

---

## 九、Agent 协作需求

### 9.1 文件优先

Agent 协作的核心不是网页内聊天，而是项目文件。

项目目录对人保持简单，主要由 HTML 和 Markdown 构成。地图布局和结构化关系可以保存在内部索引文件中，用户不需要直接维护。

建议结构：

```text
项目目录/
├── MAP.html                  # 地图应用或入口
├── MAP.md                    # 地图说明与项目概览
├── nodes/                    # 节点详情 Markdown
├── routes/                   # 路线与方案记录 Markdown
├── .live-dot-map/            # 内部结构与布局索引
└── AGENTS.md                 # 可选的项目级 Agent 指令
```

### 9.2 Skill / 插件

提供官方 skill 或插件，告诉本地 Agent：

- 活点地图的文件位置和结构。
- 节点、路线、方案、状态和标注的语义。
- 创建和更新地图的正确方式。
- 红灰方案不得连接目标节点等强制规则。
- 如何在修改后保持数据完整。

Agent 只要运行在项目目录中并加载该 skill/指令，就可以操作地图，不要求用户在网页里专门连接某一个 CLI。

### 9.3 可选本地桥接

本地桥接不是文件格式的前提，只用于增强体验：

- 网页直接发送自然语言任务给本地 Agent。
- Agent 修改后，画布实时刷新。
- 自动发现当前工作区和 Agent 状态。

没有桥接时，Agent 仍然可以通过项目文件正常工作。

### 9.4 Agent 安全边界

- Agent 默认只读写当前项目目录。
- 不把私密文件内容写入地图日志。
- 删除大量节点、路线或文件前需要人确认。
- 数据写入必须可验证、可恢复。

---

## 十、界面与体验需求

详细要求见：[UI设计需求文档.md](./UI设计需求文档.md)

### 10.1 界面方向

- Agent 首屏参考 ChatGPT Work / Codex。
- 无限画布和基础操作参考 Excalidraw。
- 属性面板和菜单参考 Linear 的克制信息层级。
- 本地文件关系参考 Obsidian Canvas。

### 10.2 必须满足

- 禁止多余文字。
- 画布是界面主体。
- 不显示常驻 Agent 聊天窗口。
- 方案状态一眼可辨。
- 节点与方案职责清楚。
- 属性面板按需出现。
- 详细信息不进入画布。
- 用户不需要理解 CLI、MCP 和数据格式才能使用。

### 10.3 原型方式

- 后续使用可交互 HTML 原型审核 UI。
- 静态 SVG 只用于简单结构示意。
- UI 通过后再进入完整功能改造。

---

## 十一、技术与产品形态

### 11.1 Excalidraw 基础

后续画布基于 Excalidraw 改造，不继续从零实现通用画布能力。

复用：

- 无限画布。
- 缩放、平移、选择和拖拽。
- 箭头控制点与绑定。
- 撤销重做和基础快捷键。
- 浮动工具与菜单的交互基础。

自有实现：

- 节点、路线、方案和标注语义。
- 三种方案状态与连接约束。
- Markdown 和内部索引。
- Agent 协作与更新反馈。
- 项目管理和属性面板。

### 11.2 Excalidraw 许可证

Excalidraw 使用 MIT License。活点地图在复用或修改其代码时必须保留对应版权和许可声明，并使用自己的产品名称和品牌。

### 11.3 本地优先

- 默认可以离线查看和编辑。
- 不要求登录账号。
- 不要求云数据库。
- 项目文件可以使用 Git、网盘或团队已有工具同步。

### 11.4 发布形态

目标仍然是轻量本地应用。可以提供单 HTML 查看/编辑入口，同时允许可选的本地连接器增强 Agent 实时协作。

单 HTML 不是阻碍核心能力的绝对限制；如果浏览器安全边界影响无感文件协作，可以增加可选桌面壳或本地桥接。

### 11.5 性能

- 常见地图打开和操作应当即时响应。
- 支持至少百级节点和方案线流畅使用。
- Agent 更新时不阻塞画布浏览。
- 大量 Markdown 不应一次性渲染到画布。

---

## 十二、非目标

第一阶段不做：

- 通用白板和自由绘画软件。
- 富文本笔记编辑器。
- Jira/Linear 式项目管理系统。
- 产品内完整聊天客户端。
- 云端模型市场和模型供应商管理。
- 强制注册、登录和云同步。
- 复杂多人实时光标协作。
- 移动端完整编辑体验。

---

## 十三、典型案例

### 13.1 项目主路线

```text
开始
  ──生成基础图片──>
生成图片
  ──实现放大──>
完成放大
  ──生成动画──>
动态壁纸完成
```

绿色方案线连接每个已经确认的结果节点。

### 13.2 幽灵脸问题路线

```text
幽灵脸问题
  ├── 修改提示词       红色失败，悬空
  ├── 更换模型         灰色待验证，悬空
  └── 引入遮罩检测     绿色成功 ──> 问题解决
```

“修改提示词”“更换模型”“引入遮罩检测”都是方案线名称，不是节点。

该问题路线可以放在项目主路线旁边，并记录它来源于“生成图片”或“完成放大”节点。

---

## 十四、成功标准

### 14.1 人类使用

- 新用户无需教程即可创建第一张地图。
- 用户可以同时理解至少三条并行路线。
- 用户可以一眼区分成功、失败和待验证方案。
- 画布在隐藏标注后仍保留完整路线语义。
- 用户无需打开长文档就能判断当前探索状态。

### 14.2 Agent 使用

- Agent 可以通过官方 skill/指令读取地图。
- Agent 可以创建节点、路线和方案。
- Agent 可以正确更新方案状态和目标关系。
- Agent 不会把失败或待验证方案连接到目标节点。
- Agent 可以读取隐藏标注和对应 Markdown。
- Agent 更新后，地图数据仍然完整并可打开。

### 14.3 产品质量

- 地图数据开放、可备份、可恢复。
- 本地使用不依赖云端服务。
- 常见操作响应流畅。
- 产品界面保持简洁，不随着功能增加变成仪表盘。

---

## 十五、开发优先级

### P0：核心可用

- 基于 Excalidraw 的无限画布。
- 圆形节点与自由布局。
- 多路线并行。
- 成功、失败、待验证三种方案线。
- 强制连接规则。
- 方案名称和标注。
- 节点/方案关联 Markdown。
- 本地保存、打开、导入、导出。
- Agent 可读写的结构化文件规范。
- 官方 skill/`AGENTS.md` 指南。
- Agent 优先首屏和手动创建入口。

### P1：无感协作

- 外部文件变化自动刷新。
- 可选本地桥接。
- Agent 更新对象高亮。
- 最近项目。
- 路线聚焦和自动整理。
- 搜索节点与方案。

### P2：团队增强

- Git 友好的变更与冲突处理。
- 地图快照和版本对比。
- 只读分享。
- 可选团队同步。

---

## 十六、现有 Demo 的处理

- 当前 ReactFlow 版本视为产品验证 Demo，不再作为最终 UI 和画布技术基础。
- 现有节点、方案状态和数据示例应尽量迁移，不直接丢弃。
- 现有 SVG 状态稿只保留为讨论记录，不作为视觉验收标准。
- 下一步先制作符合 UI 需求的可交互 HTML 原型。
- HTML 原型通过后，再基于 Excalidraw 实现完整功能和 Agent 文件协议。

---

## 十七、产品总结

活点地图的核心不是画图，而是让人和 Agent 共享同一份探索状态。

人看到的是少量节点、路线、方案和颜色；Agent 读取的是完整结构、标注和 Markdown。项目可以不断产生新的问题路线，也可以保留已经失败的尝试，而不会失去整体方向。

最终产品应当做到：

> 像 ChatGPT 一样自然地把目标交给 Agent，像 Excalidraw 一样安静地查看探索状态，并像普通本地文件一样容易保存、理解和迁移。
