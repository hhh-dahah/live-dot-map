# 设计细节 — 原型问题记录

> 用途：记录 HTML 原型（canvas.html / start.html 等）走查中发现的问题与设计细节决策，供正式产品开发参考。
> 每条记录包含：问题描述、涉及页面/位置、期望行为（正式版应如何做）、状态。

## 待确认

（暂无）

## 已确认

### 1. 取消独立首屏，直接进入画布

- **问题描述**：start.html 独立首屏（中央「想探索什么？」对话框）多此一举。用户习惯 Codex 式 CLI 对话框，不想在网页产品里对话；现阶段也不在产品内接入 Agent 对话。
- **涉及位置**：start.html（含 `?offline=1` Agent 不可用态）、index (2).html 导航。
- **正式版期望行为**：打开产品直接进入画布（Excalidraw 模式）。选择项目、打开地图、新建地图、导入地图、保存/导出等功能收进画布左上角的项目菜单（Excalidraw 式汉堡菜单面板），不做独立首屏页。空地图时的中央输入态保留在画布内（建图流程，不是独立页面）。
- **状态**：已确认，原型已同步（start.html 已删除，项目菜单移入画布左上）。

### 2. 方案线要带箭头、可自由拖拽

- **问题描述**：失败（红虚线）、待验证（灰点线）的悬空线位置固定，无法自由移动；且线没有箭头——悬空只是不指向节点，不代表没有方向。
- **涉及位置**：canvas.html 方案线渲染与交互。
- **正式版期望行为**：
  - 所有状态的方案线统一带箭头（成功连到目标节点；失败/待验证的箭头在悬空末端）。
  - 悬空端点可自由拖拽（拉长、缩短、改变方向）。
  - 线身可弯曲拖拽（控制点），用户可手动整理布局。
  - AI 创建方案线时自动布局、把控间距（见第 3 条）。
- **状态**：已确认，原型已同步。

### 3. 节点去编号、名称进圆圈；标注归属必须明确；拥挤问题靠自动布局 + 手动微调

- **问题描述**：
  1. 节点编号没有用处；
  2. 名称应直接写在圆圈内、圆自动适配文字大小（Excalidraw 已实现此模式）；
  3. 方案名、标注、线挤在一起影响思考，且看不出一条标注属于节点还是属于某条线的方案。
- **涉及位置**：canvas.html 节点渲染、方案名标签、标注。
- **正式版期望行为**：
  - 节点不显示编号（编号仍可用于底层文件命名），名称写在圆内，圆随文字自动适配（长名称自动换行）。
  - 方案名继续附着在箭头线上。
  - 拥挤问题采用「AI 创建时自动布局 + 标签自动避让（主），用户可拖拽端点/弯曲/位置微调（辅）」的组合方案；不用改变颜色来区分（绿/红/灰是状态语义，不能挪作他用）。
  - 标注必须有明确的归属视觉：靠近所属对象，并用细引导线连到所属节点边缘/方案线中点，一眼可辨属于谁。
- **状态**：已确认，原型已同步（轻量避让 + 引导线）；正式版的 AI 自动布局算法另行设计。

### 4. 标注改黄色小便签 + 避让升级；方案线线身直接弯折

- **问题描述**：
  1. 标注为纯文字 + 灰圆点，位置固定偏移且无碰撞检测，压在方案线上、与方案名混排，归属一眼看不清；
  2. 方案线弯曲只能抓紫色控制点手柄，不能直接拖线身，操作门槛高。
- **涉及位置**：canvas.html 标注渲染与交互、方案线交互；brand-spec.md 色彩令牌；UI设计需求文档 §11。
- **正式版期望行为**：
  - 标注用黄色小便签包裹（人与 Agent 统一），使归属与方案线、方案名天然区隔；便签黄（--note）仅用于标注，不挪用至其它元素。此决策推翻旧约“标注不使用便签卡、彩色气泡”，理由：归属辨识度优先于绝对中性。
  - 便签位置自动避让：节点标注 8 方向候选打分（压方案线罚分最高，右侧优先）；方案线标注在曲线 t=0.5 方案名另一侧，压线沿法向外移；引导线 opacity 提升到 .45，间距不小于 26px。
  - 便签可拖拽微调（偏移存入标注数据），双击复位回自动位置；正式版延续“自动避让为主、手动微调为辅”。
  - 方案线选中后可直接按住线身弯折：曲线经过指针（由 B(0.5) 反推控制点），紫色手柄仅作视觉指示。
- **状态**：已确认，原型已同步；完整避让算法与精确控制点逻辑在正式版（Excalidraw 基础）另行实现。
