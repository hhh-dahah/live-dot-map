# 设计细节 — 原型问题记录

> 用途：记录 HTML 原型（canvas.html 等）走查中发现的问题与设计细节决策，供正式产品开发参考。
> 每条记录包含：问题描述、涉及页面/位置、期望行为（正式版应如何做）、状态。

## 待确认

（暂无）

## 已确认

### 1. 取消独立首屏，直接进入画布

- **问题描述**：start.html 独立首屏（中央「想探索什么？」对话框）多此一举。用户习惯 Codex 式 CLI 对话框，不想在网页产品里对话；现阶段也不在产品内接入 Agent 对话。
- **涉及位置**：start.html（含 `?offline=1` Agent 不可用态）、index (2).html 导航。
- **正式版期望行为**：打开产品直接进入画布（Excalidraw 模式）。选择项目、打开地图、新建地图、导入地图、保存/导出等功能收进画布左上角的项目菜单（Excalidraw 式汉堡菜单面板），不做独立首屏页。空地图时的中央输入态保留在画布内（建图流程，不是独立页面）。
- **状态**：已确认，原型已同步（start.html 已删除，项目菜单移入画布左上）。注：空地图中央输入态已在第 5 条中取消，新建地图直接进空白画布。

### 2. 方案线要带箭头、可自由拖拽

- **问题描述**：失败（红虚线）、待验证（灰点线）的悬空线位置固定，无法自由移动；且线没有箭头——悬空只是不指向节点，不代表没有方向。
- **涉及位置**：canvas.html 方案线渲染与交互。
- **正式版期望行为**：
  - 所有状态的方案线统一带箭头（成功连到目标节点；失败/待验证的箭头在悬空末端）。
  - 悬空端点可自由拖拽（拉长、缩短、改变方向）。
  - 线身可弯曲拖拽（控制点），用户可手动整理布局。
  - AI 创建方案线时自动布局、把控间距（见第 3 条）。
- **状态**：已确认，原型已同步。

### 3. 节点去编号、名称进圆圈；标注归属必须明确；拥挤问题靠自动布局 + 手动微调

- **问题描述**：
  1. 节点编号没有用处；
  2. 名称应直接写在圆圈内、圆自动适配文字大小（Excalidraw 已实现此模式）；
  3. 方案名、标注、线挤在一起影响思考，且看不出一条标注属于节点还是属于某条线的方案。
- **涉及位置**：canvas.html 节点渲染、方案名标签、标注。
- **正式版期望行为**：
  - 节点不显示编号（编号仍可用于底层文件命名），名称写在圆内，圆随文字自动适配（长名称自动换行）。
  - 方案名继续附着在箭头线上。
  - 拥挤问题采用「AI 创建时自动布局 + 标签自动避让（主），用户可拖拽端点/弯曲/位置微调（辅）」的组合方案；不用改变颜色来区分（绿/红/灰是状态语义，不能挪作他用）。
  - 标注必须有明确的归属视觉：靠近所属对象，并用细引导线连到所属节点边缘/方案线中点，一眼可辨属于谁。
- **状态**：已确认，原型已同步（轻量避让 + 引导线）；正式版的 AI 自动布局算法另行设计。

### 4. 标注改黄色小便签 + 避让升级；方案线线身直接弯折

- **问题描述**：
  1. 标注为纯文字 + 灰圆点，位置固定偏移且无碰撞检测，压在方案线上、与方案名混排，归属一眼看不清；
  2. 方案线弯曲只能抓紫色控制点手柄，不能直接拖线身，操作门槛高。
- **涉及位置**：canvas.html 标注渲染与交互、方案线交互；brand-spec.md 色彩令牌；UI设计需求文档 §11。
- **正式版期望行为**：
  - 标注用黄色小便签包裹（人与 Agent 统一），使归属与方案线、方案名天然区隔；便签黄（--note）仅用于标注，不挪用至其它元素。此决策推翻旧约“标注不使用便签卡、彩色气泡”，理由：归属辨识度优先于绝对中性。
  - 便签位置自动避让：节点标注 8 方向候选打分（压方案线罚分最高，右侧优先）；方案线标注在曲线 t=0.5 方案名另一侧，压线沿法向外移；引导线 opacity 提升到 .45，间距不小于 26px。
  - 便签可拖拽微调（偏移存入标注数据），双击复位回自动位置；正式版延续“自动避让为主、手动微调为辅”。
  - 方案线选中后可直接按住线身弯折：曲线经过指针（由 B(0.5) 反推控制点），紫色手柄仅作视觉指示。
- **状态**：已确认，原型已同步；完整避让算法与精确控制点逻辑在正式版（自研 app.html）另行实现。

### 5. Excalidraw 式交互细节：悬浮发光、抓取点弯折、便签尾巴与选中编辑、框选删除、拉出式箭头、新建地图直进画布

- **问题描述**：
  1. 对象无悬浮反馈，不知道能不能交互；
  2. 弯折时曲线整体跳动，抓取点不跟手（Excalidraw 是中间点固定弯折）；
  3. 便签用细引导线连对象太丑，应为微信气泡式小角角；便签无选中态、不能直接改字；
  4. 「新建地图」不应再出现中央输入画面，应直接进空白画布，选择项目在地图中进行；
  5. 无框选与一键删除；
  6. 新建方案线要先点起点再点终点，反人类；应在节点上按住直接拉出，终点拖到节点上自动吸附变绿，没拖到就保持悬空灰色。
- **涉及位置**：canvas.html 全部交互；UI设计需求文档 §5.4/§6/§11/§14/§16。
- **正式版期望行为**（均对标 Excalidraw，正式版在自研 app.html 实现时延续）：
  - 鼠标悬浮节点/方案线时紫色发光（--accent-soft 光晕），提示可交互。
  - 弯折：按住线身或中点手柄时，抓取点固定在指针下，其余部分随之变形（按曲线参数 t 反推控制点）。
  - 便签归属用四角的小尾巴（小角角）连向所属对象，拖动便签时尾巴随距离缩放拉伸；不用细引导线。
  - 便签点击变紫色选中态，再次点击直接行内编辑；拖动微调位置。
  - 新建地图直接进空白画布，不设中央输入态；选择项目在地图中通过项目菜单完成。
  - 空白处拖拽出紫色框选，选区右上角出现一键删除按钮；Delete 键同样生效。平移改用滚轮/中键/空格。
  - 箭头工具：在节点上按住直接拉出方案线，起点自动吸附；拖动终点命中另一节点时高亮预览，松手自动吸附连接并变绿（标为成功）；未命中则保持悬空灰色点线（待验证）。已连接端点也可拖起重新布线。
- **状态**：已确认，原型已同步；这些是交互姿态决策，正式版在自研 app.html 上实现时需真实落地。

### 6. 手掌工具、拖末端前半段固定弯折、绿线末端可拖下变灰

- **问题描述**：
  1. 工具条缺手掌工具，平移只能靠空格/中键/滚轮，不直观；
  2. 拖动箭头末端时整条曲线随之平移摆动；期望前半段保持固定、后半段自由弯折，拖线身后半段时前半段适应性跟随，保持优美曲线；
  3. 绿色已连接线末端无法拖下——应能拖起脱钩，重新布线或变灰悬空。
- **涉及位置**：canvas.html 工具条、方案线交互；UI设计需求文档方案线小节。
- **正式版期望行为**（对标 Excalidraw）：
  - 工具条第一位为手掌工具（快捷键 H），选中后拖动画布任何位置都是纯平移，节点/线不可拖；光标 grab/grabbing。
  - 拖动方案线末端时钉住曲线前段锚点（约 t=0.25 处），前半段基本固定，后半段自由弯折；拖线身弯折沿用抓取点固定算法，前半段自然适应性跟随，曲线保持平滑。
  - 已连接（绿色）方案线选中或悬停时末端出现手柄，拖起即脱钩回到待验证（灰色点线悬空）；拖到别的节点松手重新吸附变绿。
- **状态**：已确认，原型已同步；正式版在自研 app.html 上实现时延续此交互姿态。

### 7. 连续拉箭头、方案名双击编辑、自动命名、面板底部可隐藏内部编号

- **问题描述**：
  1. 箭头工具拉完一条线后自动切回选择工具，无法连续拉多条；
  2. 从节点拉出新方案时，线中间的弯曲手柄不在节点与末端的中点上，看着别扭；
  3. 方案名只能在右侧面板里改，无法双击画布上的方案名直接编辑；
  4. 新建节点/方案都叫“新节点”“新方案”，重名无法区分；
  5. 节点一多后需要内部编号向数据库方向靠，但画布上不显示（见第 3 条）。
- **涉及位置**：canvas.html 箭头工具交互、方案名标签、addNodeAt/addEdge/setEdgeStatus、属性面板、设置抽屉。
- **正式版期望行为**：
  - 箭头工具拉完一条线后保持箭头工具，可连续拉多条；Esc 或手动切换才离开。
  - 新建边拉出过程中保持直线（不套用已存在边重布线的前段锚点算法），弯曲手柄自然位于中点。
  - 双击画布上的方案名标签即弹出行内输入框，Enter 保存、Escape 取消，Markdown 文件名同步更新；面板内改名同样同步文件名。
  - 新建节点默认命名“新节点1/2/3…”，新建方案默认命名“方案1/2/3…”（含状态切换自动创建的目标节点），计数器随撤销/重做快照恢复。
  - 内部编号（节点 num、方案取边 id 数字）以极小字（10px 灰）显示在属性面板底部，不占正式字段；默认隐藏保持简洁，开关收进工具条三个点菜单（“显示内部编号”）。
- **状态**：已确认，原型已同步。

### 8. 普通节点升级为问题路线；路线名/节点名双击编辑；节点类型自定义

- **问题描述**：
  1. 新建的普通节点需要一个入口升级为问题路线的起点（右键菜单与属性面板都要有按钮）；
  2. 画布上的路线名（紫色文字）与节点内文字无法直接改，只能进面板；
  3. 节点类型在生产场景下很多（问题、结果、里程碑、方案一效果等），面板里不可改。
- **涉及位置**：canvas.html 节点右键菜单、属性面板、双击交互、createIssueRoute 重写为 promoteToIssueRoute。
- **正式版期望行为**：
  - 普通节点可就地升级为问题路线起点（右键菜单“升级为问题路线” + 面板“升级成问题路线”按钮）：节点类型变为问题，新建以其命名的路线，直连的方案线与对端节点并入该路线；已是路线起点的节点不重复升级。以后该节点连接的节点、方案都属于这个路线。
  - 路线名、节点名、方案名均支持双击行内编辑，Enter 保存、Escape 取消；节点改名同步 Markdown 文件名，半径随文字自动重算。
  - 节点类型为自由文本，面板点击即可自定义修改。
  - AI 自动建点时根据用户意图生成 里程碑/问题/结果/自由 四种类型之一（正式版 Agent 能力，原型阶段类型由用户手定）。
- **状态**：已确认，原型已同步。

### 9. 点击生成方案线自动避让、拖拽保持自由、光标统一、方案编号按节点重置、升级路线命名

- **问题描述**：
  1. 箭头工具下**点击节点（未拖拽）**自动生成的默认方案线方向固定，连续点击多次全部叠在一条线上；`staggerParallel`（同目标平行线错开）定义了但从未被调用。
  2. 箭头模式下光标偶尔"消失"：代码无 `cursor:none` 也无全屏覆盖层，排查结论为节点 `grab` / 线 `pointer` / 手柄 `grab` / 空白 `crosshair` 在拖线时频繁切换，细十字在深色背景快速移动时视觉上像消失。
  3. 方案名全局递增（方案13/14/15…），用户要求每个节点自己的方案从 1 开始数，区分靠内部编号。
  4. 节点升级为问题路线后路线名复制节点名（如"新节点7"）且无法改；期望默认"分支路线1/2/3…"并支持双击改名。
- **涉及位置**：canvas.html 箭头工具、`addEdge`、`avoidAngleDeg`、`staggerParallel`、`promoteToIssueRoute`、属性面板"所属路线"字段、`#viewport.tool-place` 光标。
- **正式版期望行为**：
  - **点击节点（未拖拽）**自动生成的默认方案线与同节点已有线保持 ≥30° 夹角自动错开，连续点击多次依次排开不重叠；**拖拽拉线 / 拖拽已有线末端重布线保持自由自定义方向，不做角度限制**（方向完全跟随指针）；连到同一目标节点的多线沿法线按索引对称错开 46px。
  - 箭头/新建节点等非选择工具下，画布上节点、线、手柄光标统一为 `crosshair`，避免拖线时光标样式频繁切换造成的"闪烁消失"观感；正式版若仍偶发消失再排查显卡/缩放渲染。
  - 方案名从该起点节点的第 1 条线开始编号（方案1/2/3…），删除或改名不重排已有方案；区分用内部编号（面板底部 `#编号`）。
  - 升级为问题路线默认命名"分支路线N"（计数器进撤销快照）；画布路线名与属性面板"所属路线"均支持双击行内改名，Enter 保存、Escape 取消。
- **状态**：已确认，原型已同步。

### 10. 状态与连接解耦、右键「新建连接节点」、隐藏失败方案

- **问题描述**：
  1. 原规则强制"只有绿色（成功）线才能连接节点"：标记成功时自动创建目标节点、标记失败/待验证时强制脱钩。用户思考方式是"从起点提出几个方案，连不连节点自己定"——红色/灰色线也应可连接节点，连接与否由用户拖拽或按钮决定，与颜色状态无关。
  2. 悬空方案线（灰/红）没有快捷方式补上目标节点，只能手动建节点再拖过去吸附。
  3. 画布上红色（失败）方案线一多视觉噪音大，需要一键隐藏。
- **涉及位置**：canvas.html `setEdgeStatus`、右键菜单 `edgeMenu`、新函数 `createTargetNode`、三个点菜单（新增"显示失败方案"开关）、`render()` 渲染过滤。
- **正式版期望行为**：
  - **状态与连接解耦**：绿/红/灰三种状态均可连接或不连接节点；标记状态只改颜色不动连接；连接与否由拖拽（端点拖到节点吸附 / 拖起脱钩）和「新建连接节点」决定。注：推翻第 7 条"状态切换自动创建目标节点"与第 6 条"绿线拖起脱钩后必变灰"中关于"状态随连接强制变化"的部分（脱钩后仍保持原状态可选）。
  - 右键悬空方案线菜单新增「新建连接节点」：自动在悬空端点创建"结果"类型节点并连接，方案线保持原状态不变。
  - 三个点菜单新增「显示失败方案」开关（默认开）：关闭后画布隐藏全部红色失败线（含标注避让与框选排除），数据不删除。
- **状态**：已确认，原型已同步。

### 11. 选择模式双击编辑修复、属性面板字段统一

- **问题描述**：
  1. 选择模式下双击节点/方案名/路线名无法编辑，而手掌模式下可以。根因：选择模式点击选中时 `select()` 同步 `render()` 重建 DOM，事件目标被销毁，浏览器不再派发 `click`/`dblclick`（事件链断裂）；方案名标签还因命中 `bend` 拖拽分支（标签在 `[data-edge]` 组内）额外叠加一层问题。
  2. 节点与方案面板字段顺序不一致（节点：名称→类型→所属路线；方案：方案名称→状态→起点→…→所属路线）；所属路线无关联时显示"—"，语义不清；新建节点默认归入第一条路线（`route:S.routes[0]`），用户希望新建默认"无"。
  3. 所属路线字段只读，无法把节点/方案改挂到其它路线。
- **涉及位置**：canvas.html pointerdown/pointerup 选择与弯折分支、`world` dblclick 处理、`addNodeAt`、`renderPanel` 字段、新增 `routePickHTML`/`routeNameOf`、`lastClick` 手动双击检测。
- **正式版期望行为**：
  - 选择模式下双击可编辑与手掌模式一致：节点名、方案名（线身标签）、路线名均支持双击行内编辑，Enter 保存、Escape 取消。实现方式：点击选中的渲染延迟到 rAF 保持 DOM 稳定 + 标签双击用手动检测（按对象 key，元素实例会随渲染重建，不能比较引用）。
  - 节点与方案属性面板字段顺序统一：**所属路线置顶**，其次名称（方案为"方案名称"）、类型/状态，其后是 Markdown、标注等。
  - 所属路线显示规则：与已命名路线有关联的显示路线名，无关联显示"无"；新建节点默认"无"（不再默认归入第一条路线）；新建方案线跟随起点节点所属路线。
  - 所属路线字段支持点击下拉：列出「无」与全部已有路线供选择（勾选当前），并附「重命名当前路线…」入口；画布路线名双击改名保留。
- **状态**：已确认，原型已同步。

## Excalidraw 版复刻对照（阶段 2，2026-08-06）

> Excalidraw fork（D:/桌面/活点地图/excalidraw）复刻状态：已复刻 = 交互与视觉对齐；差异 = 记录差异说明，待阶段 3 或走查后定夺。

- 1（无首屏）：已复刻——移除欢迎屏，打开即画布。
- 2（三态线/悬空端/线身弯折）：已复刻（三态颜色线型、端点吸附/脱钩）；差异——线身弯折为 Excalidraw 双击进点编辑（原型为按住弯折）。
- 3（节点去编号/圆内名称/标注避让）：已复刻（正圆自适配、4 字换行、annScore 避让）；差异——便签无归属小尾巴。
- 4（便签/自动避让）：已复刻（黄底便签+微旋转+打分避让）；差异——双击复位未实现（可拖动微调）。
- 5（悬浮反馈/拉出式新建/框选删除）：已复刻（Excalidraw 原生悬浮与选中高亮、L 工具拉出、框选+Delete）；差异——选中用紫蓝框（原生），无悬浮光晕。
- 6（手掌工具/拖末端弯折/脱钩）：已复刻（H 手掌原生、端点拖拽脱钩原生）。
- 7（悬空端点常显手柄）：差异——Excalidraw 悬空端选中后显示手柄，非常显（视觉差异，可接受）。
- 8（隐藏失败方案/内部编号）：已复刻（开关+dm.hidden 数据保留、面板底部编号可隐藏）。
- 9（光标 crosshair）：已复刻（Excalidraw 工具光标，方向一致非像素级）。
- 10（状态连接解耦/新建连接节点/隐藏失败）：已复刻（三态可连可悬空、右键新建连接节点、显示失败方案开关）。
- 11（双击编辑/面板字段统一）：已复刻（双击行内编辑 isComposing 保护、面板字段 所属路线→名称→类型/状态→其余）；差异——双击编辑为 Excalidraw 原生文本编辑（Enter/Esc 原生）。

## Excalidraw 版走查（2026-08-06，wip-stage2 分支，deepseek 实现）

> 对上面「复刻对照」的实测复核（Kimi 走查，截图在 `走查截图/s2-*.png`）。结论：deepseek 报告的「已复刻」大面积高估，以下为实测问题，按严重度分级。

### P0 阻塞/崩溃

- **W1 选中崩溃**：场景含残缺元素（手搓元素缺 `groupIds` 等基础字段，如 demoScene 里的路线名标签/方案名标签/便签）时，点击选中触发 `groups.ts` `TypeError: Cannot read properties of undefined (reading 'find')`，整个 App 被错误边界卸载成白屏。
- **W2 L 工具点击流程断裂**：L 点击节点 = 原生多点箭头的「开始」，不断开；之后的任何点击/拖拽都被吸进同一条线（实测拖拽「替代路径→结论」变成了给方案5 续点）。没有实现「点击生成默认线 + ≥30° 角度避让」（走查 9）。
- **W3 吸附不变绿**：新线终点吸附节点后状态仍 pending 灰点线；走查 5/PRD v2.1 §5.4 要求新线吸附默认标成功。
- **W4 示例图几何退化**：demoScene 手搓箭头 `width/height=0`，全部渲染成水平直线（忽略 dy）；平行线未错开；方案名标签位置错乱；初次注入时曾整组不渲染（崩溃重挂载后才出现）。

### P1 功能缺陷

- **W5 面板数据错**：方案名称显示「—」（读不到线上绑定文本）；所属路线显示原始 routeId（`r-demo-main`）而非路线名。
- **W6 节点类型被砍成两个固定项**（目的/问题），违反走查 8「类型自由文本」；deepseek 报告自称已改 PRD 为「类型二类」，实际 PRD 未被动过（仍是 v2.1 自由文本），实现与报告双向不实。
- **W7 隐藏失败方案非响应式**：开关只在做切换动作时打 `dm.hidden` 一次；开关开着时新标记为失败的线仍然显示（实测复现）。
- **W8 便签**：无归属小尾巴（走查 5）；落点压节点、压线（避让打分未生效或参数不对，实测便签直接盖住节点右缘和红虚线）；创建后不进入行内编辑（走查 4/原型为立即编辑）；无双击复位。
- **W9 路线计数冲突**：节点升级出的新路线与示例已有路线同名（两个「分支路线1」），计数器未纳入已有路线。
- **W10 新建节点命名格式错**：「新节点 05」（带空格零填充），原型为「新节点5」。

### P2 外壳/视觉

- **W11 原生提示泄漏**：M 工具创建便签后顶部出现 Excalidraw 原生提示「按回车键添加文本，按 Ctrl+↓↓ 创建流程图」。
- **W12 右上角分享按钮（紫色）与库按钮残留**：产品无协作分享，应移除；紫色被挪用。
- **W13 左下不明橙色徽章**（! 0 ⇕ 28）：疑似 Excalidraw 离线/协作指示残留，需查清去除。
- **W14 Markdown 文件名缺名称**：`routes/e5.md`，原型为 `routes/e5-方案5.md`（节点侧 `nodes/05-新节点 05.md` 格式对但名称格式错，见 W10）。

### 实测通过项（与报告一致）

- 右键菜单（节点：升级/标注/打开 MD/删除；线：三态/新建连接节点等）✓
- 主菜单与视图开关（显示标注/路线名/失败方案/内部编号、适应视图）✓
- N 工具建点、建后切回选择工具 ✓
- 升级为问题路线（变虚线+建路线+面板切换）✓（除 W9 重名）
- 删除节点级联（指向线变灰悬空、出发线随删）✓
- 面板三态 seg 切换只改色不动连接 ✓
- 隐藏失败方案开关本身有效（一次性）✓
- 正圆节点、名称圆内、4 字换行 ✓（换行边界有个别不一致，随 W10 一并修）


### 修复销号（2026-08-06 第二轮，Kimi，均在 wip-stage2 上 Playwright 实测通过）

- **W1 ✓**：demoScene 全部改走元素工厂（createNodePair/newTextElement），无残缺元素，选中不再白屏。
- **W2 ✓**：L 工具点击节点生成默认线（0 长度箭头打标时修正，方向与同节点已有线 ≥30° 避让，长 220）；拖拽拉线流程不再被吸进同一条线。
- **W3 ✓**：binding 变化监听（syncEdgeStatusWithBinding）自动翻转 待验证↔成功——实测脱钩立即变灰点线、重新吸附变回绿实线；失败状态不自动碰。
- **W4 ✓**：demoScene 几何重写——正圆节点、平行线 46px 错开、方案名标签附着线中，注入即正确渲染。
- **W5 ✓**：面板方案名读 bound text、所属路线显示注册表名称。
- **W6 ✓**：类型恢复自由文本（「问题」触发虚线边框，实测 问题↔目的 切换 dashed↔solid 即时生效）；面板字段/样式重做。
- **W7 ✓**：状态变更收口处（setEdgeStatus、面板 setElementDm）按「显示失败方案」开关同步 dm.hidden——开关关时标记失败立即消失、改回待验证立即重现；删除级联（指向线变 pending）同时清 hidden 残留。截图 `s2-22-W7隐藏失败.png`。
- **W8 ✓**：便签黄色小尾巴（节点目标锚圆边缘、线目标锚中点，`dotmap/annTail.ts` 挂 staticScene 渲染）；创建即进入行内编辑；避让打分改用实测文本宽高；未选中双击=回到自动避让位置、已选中单击=进编辑（本地双击判定，不依赖 lastPointerUpIsDoubleClick）；bound text 存储坐标置中修复（选中再单击打不中文字的根因）。
- **W9 ✓**：分支路线编号=已有「分支路线N」最大值+1；并修复衍生问题——路线注册表是内存态，刷新后丢失导致重名与面板丢名，现 onChange 里随场景持续重建（rebuildRoutesRegistry）。
- **W10 ✓**：显示名「新节点5」不补零（补零编号仅内部 num 用）。
- **W11 ✓**：arrow 工具（即方案线 L）提示改为原型文案「在节点上按住拖拽拉出方案线…」，不再泄漏原生提示。截图 `s2-23-W11W12W13外壳.png`。
- **W12 ✓**：renderTopRightUI 置空——右上分享/协作触发器与 Excalidraw+ 推广不再渲染。
- **W13 ✓**：查明为 vite-plugin-checker 的 dev 错误/警告徽章（非应用 UI，生产构建本无），`vite.config.mts` 关 overlay（终端仍输出检查）。
- **W14 ✓**：面板 Markdown 路径 `routes/e{num}-{名称}.md`（节点侧 `nodes/{num}-{名称}.md`）。

### 第二轮补充实测通过项

- 节点类型自由文本切换虚线边框 ✓；端点脱钩/回吸状态翻转 ✓；面板标注隐藏↔显示往返 ✓、删除 ✓；删除节点级联（入边灰点悬空、出边随删）✓；右键菜单节点（含升级为问题路线）/方案线（三态+标注+MD+删除）✓；RoutePicker 单行显示（名称+▾ 不换行）✓。

### 仍存在的已知差异（接受，暂不改）

- SVG/PNG 导出不含便签小尾巴（尾巴只在 canvas 交互渲染层）。
- 便签长文本按 Excalidraw 原生换行增高（原型为单行增宽）。
- 线身弯折用 Excalidraw 双击进入点编辑（原型为按住拖拽弯折）。

---

## 阶段 5 模拟用户走查（2026-08-07，Playwright 模拟新用户全流程）

> 范围：app.html 正式版的分发与上手工程（首次引导 / agent-kit / PWA / 托管）。问题按「问题描述 / 涉及位置 / 期望行为 / 状态」记录。

### 已修（本轮销号）

- **S5-1 演示模式静默不落盘**（已修）：未连接文件夹时编辑画布，改动只存内存，刷新即丢，界面毫无提示。涉及 `scheduleSave()` 的 `!IO.mapFile` 分支。期望：首次编辑即提示。现状：已加一次性 toast「演示模式：改动只在本页，连接文件夹或导出 map.json 才能保存」（`IO.demoHinted` 置位），Playwright 实测触发。
- **S5-2 PWA 缓存导致更新不生效**（已修）：sw.js v1 对所有请求缓存优先，旧 app.html 占缓存后新代码死活不生效（排查演示提示不生效的根因就是它）。期望：HTML 永远拿最新、离线才回落缓存。现状：sw.js v2 改「HTML 网络优先 + 静态资源缓存优先」，实测 manifest/sw 注册正常。教训：本地调试若怀疑旧缓存，先清 SW + CacheStorage 再刷新。

### 已修（第二轮销号）

- **S5-6 设置抽屉「演戏内容」**（已修）：设置抽屉里原有深色模式/默认文件夹/工作流保存等占位开关，点了没实际功能（纯演戏）。涉及 app.html 设置抽屉。期望：做不到的功能不摆出来。现状：已改为诚实的「Agent 接入」指引块 + syncDrawerFolder 显示真实文件夹连接状态。
- **S5-7 「交给 Agent」假模拟器**（已修）：原 submitAgent 按钮点了播放一段假的 Agent 思考动画，不产生任何真实输出。期望：要么真接要么别装。现状：已删除模拟器，按钮改「接入 Agent」直接打开接入抽屉。
- **S5-8 agent-kit 的 md 公网访问乱码**（已修本地，公网待下次部署）：`.deploy/_headers` 原来一刀切 text/plain，浏览器直接当文本流显示且中文乱码。现状：_headers 按扩展名分流（.md 给 charset=utf-8 的可下载类型），并为 agent-kit/ 写了 index.html 说明页（公网打开 agent-kit 路径有可读页面）。注意：公网跑的还是旧规则，下次 wrangler deploy 后生效。

### 遗留（接受或待办）

- **S5-3 托管首页是裸跳转页**：`.deploy/index.html` 只是 meta refresh 到 app.html，新用户打开网址没有任何「这是什么/怎么用/Agent 怎么接」的引导。期望：正式分发前改造成落地页（可基于 landing.html，加下载、三分钟教程、agent-kit 链接）。状态：待办（阶段 5 本轮先拿通网址，落地页下一步做）。
- **S5-4 路线级归档无画布入口**（阶段 4 遗留）：schema/协议已支持 `archived`，数据里壁纸项目已归档 8 条，但画布上没有「归档路线」的查看/恢复入口。期望：项目菜单或面板提供归档列表。状态：待办。
- **S5-5 文件夹直连+轮询同步无法自动化走查**：FS Access 授权必须用户手势，Playwright 无法覆盖「连文件夹→Agent 改 map.json→画布 2 秒自刷新」的完整链路。状态：留给人工验收（日常使用第一高频路径，务必真人过一遍）。
