# 活点地图 UI 对标与界面需求

> 本文只定义产品界面方向、对标参考和必须满足的体验要求。  
> 不规定具体像素、字号、色值、阴影和组件实现参数。  
> 后续通过可交互 HTML 原型确定细节，不再以静态 SVG 作为主要审核方式。

---

## 1. 界面目标

活点地图的界面必须让用户把注意力放在“探索结构”上，而不是工具本身。

界面应当做到：

- 第一眼看到当前探索路线、关键节点和方案状态。
- 空地图时，可以直接把目标交给 Agent。
- 有地图时，Agent 退到后台，画布成为主角。
- 人只在需要时操作节点、路线和标注。
- 详细内容保存在 Markdown 中，不堆在画布上。
- 没有正在进行的操作时，大部分控件和面板应当隐藏。

整体气质：**安静、克制、清楚、轻量、可信赖。**

---

## 2. 一级对标产品

### 2.1 ChatGPT Work / Codex

参考：[ChatGPT Work and Codex](https://help.openai.com/en/articles/20001275-chatgpt-work-and-codex)

主要借鉴：

- 首屏只提出一个简短问题。
- 自然语言输入是最醒目的操作。
- 当前项目或工作区靠近输入区域，用户不需要先学习配置。
- Agent 能力通过使用过程体现，不用说明文字解释。
- 大量留白，普通状态下几乎没有界面噪声。

不借鉴：

- 聊天历史侧栏。
- 模型选择、语音、联网权限和建议任务。
- 持续占据画布的聊天消息。

### 2.2 Excalidraw

参考：[Excalidraw](https://github.com/excalidraw/excalidraw)

主要借鉴：

- 无限画布占据整个窗口。
- 工具条悬浮在画布上方，不形成固定页面顶栏。
- 菜单、缩放、撤销等辅助操作分布在边角。
- 控件简洁、轻量，不与画布内容争夺注意力。
- 平移、缩放、选择、拖拽和箭头磁吸具有自然手感。

不借鉴：

- 手绘抖动风格。
- 自由绘画和大量图形工具。
- 图形库、素材库和分享协作功能。
- 自由颜色、填充和线宽设置。

---

## 3. 辅助对标产品

### 3.1 Linear

参考：[How we redesigned the Linear UI](https://linear.app/now/how-we-redesigned-the-linear-ui)

借鉴：

- 属性面板信息紧凑、层级清楚。
- 菜单和控件对齐准确。
- 使用中性色降低视觉噪声。
- 重要内容明显，辅助界面主动退后。

不借鉴：

- 常驻项目管理侧栏。
- 大量字段、筛选器和工作流配置。

### 3.2 Obsidian Canvas

参考：[Obsidian Canvas](https://obsidian.md/help/plugins/canvas)

借鉴：

- 本地文件优先。
- 节点与 Markdown 文件关联。
- 自由布局和跨内容连接。

不借鉴：

- 在画布节点中直接展示整篇笔记。
- 大尺寸内容卡片和媒体墙。

### 3.3 OpenDesign

参考用户提供的 OpenDesign 截图。

借鉴：

- 项目上下文明确。
- Agent 或执行方式集中管理。
- 本地工具可以被检测和使用。

不借鉴：

- 首次进入就展示大型配置窗口。
- 在主流程中暴露大量 CLI、模型和服务商选项。

---

## 4. 界面组合原则

```text
ChatGPT 的 Agent 入口
        +
Excalidraw 的无限画布
        +
Linear 的属性面板
        +
Obsidian 的本地文件关系
```

活点地图不是这些产品的拼接，而是从它们各自最适合的部分建立自己的界面：

- 用 ChatGPT 的方式开始任务。
- 用 Excalidraw 的方式操控画布。
- 用 Linear 的方式查看少量结构化属性。
- 用 Obsidian 的方式把详细信息留在本地文件中。

---

## 5. 最重要的界面原则

### 5.1 禁止多余文字

- 一个区域只保留一个标题。
- 不同时使用标题、副标题、说明、帮助提示和重复按钮。
- 图标可以表达的常用操作，不常驻显示文字。
- 技术实现只在设置和文档中解释。
- 不使用营销话术、感叹号和 emoji 表达产品状态。
- 状态发生变化时才显示提示，平时不显示“已保存”“Agent 空闲”等文字。

### 5.2 空白就是界面

- 画布默认纯净，不使用装饰背景。
- 网格可以作为设置选项，但默认不显示。
- 不使用大面积渐变、插画、纹理和卡片堆叠。
- 不为了填满页面而增加控件或说明。

### 5.3 状态颜色只表达状态

- 绿色只表示方案成功。
- 红色只表示方案失败。
- 灰色只表示待验证或不可用。
- 普通操作、Agent 提交和选中状态使用中性或独立交互色。

### 5.4 Agent 是能力，不是聊天窗口

- 空地图时，Agent 通过中央输入区出现。
- 地图建立后，Agent 入口缩小并退到边角。
- Agent 工作时只显示简短状态。
- Agent 的结果直接变成节点、路线和方案变化。
- 画布不展示 Agent 的推理过程和聊天记录。

---

## 6. 空地图与项目入口

不设独立首屏页，打开产品直接进入画布（Excalidraw 模式）。

空地图时，画布中央出现输入区，只承担两件事：描述目标、选择项目。

应当包含：

- 一个简短问题，例如“想探索什么？”。
- 一个自然语言输入区。
- 当前项目或“选择项目”入口。
- 一个提交给 Agent 的操作。

打开地图、新建地图、导入地图、保存与导出收进画布左上角的项目菜单，不做独立页面。

不应当包含：

- Agent 装饰图标。
- 副标题和功能解释。
- 单独的“让 Agent 开始”大文字按钮。
- 工作区说明和技术提示。
- 建议任务列表。
- 可见的 CLI、MCP、skill、JSON 等术语。

用户提交目标后，不进入聊天页。Agent 开始建立地图，并在第一个节点出现后直接进入画布。

---

## 7. 画布界面需求

- 画布始终是窗口主体。
- 不设置贯穿全宽的固定顶栏。
- 左上提供项目和设置入口。
- 顶部中央保留少量核心工具。
- 缩放、撤销和重做放在边角。
- 未选择对象时，不显示属性面板。
- Agent 更新提示只在发生变化时短暂出现。

核心工具只包括：

- 选择和移动。
- 新建节点。
- 新建方案线。
- 添加标注。
- 更多操作。

自由绘图、通用几何图形、图片和素材库不属于产品主界面。

---

## 8. 项目与最近项目

- 项目列表不常驻占用画布空间。
- 用户可以从菜单打开最近项目。
- 首屏输入区附近显示当前项目。
- 没有当前项目时，提示选择项目文件夹。
- 打开项目后，界面不持续显示项目配置说明。
- skill、Agent 配置和本地桥接统一放入设置，不进入日常画布流程。

---

## 9. 节点需求

- 节点统一使用圆形。
- 节点代表目的、问题或结果，不代表具体方案。
- 节点不显示编号；短名称直接写在圆内，圆随文字自动适配大小，长名称自动换行。
- 节点不展示长文本、日期、进度、负责人和 Markdown 摘要。
- 节点本身不使用红绿灰表达成功或失败。
- 节点可以自由移动和缩放，但保持圆形。
- 选中和 Agent 刚更新的节点可以短暂显示交互高亮。

---

## 10. 路线与方案线需求

- 同一画布允许多条探索路线并行存在。
- 路线可以是项目主路线，也可以是为解决某个问题创建的独立路线。
- 路线名称保持低强调，可以按需隐藏。
- 方案名称必须显示在线上，不能显示成节点。

三种方案状态：

- 成功：绿色实线带箭头，必须连接起点和目标节点。
- 失败：红色虚线带箭头，从起点发出，末端悬空，不能连接目标节点。
- 待验证：灰色点线带箭头，从起点发出，末端悬空，不能连接目标节点。

方案线交互：

- 悬空端点可以自由拖拽（拉长、缩短、改变方向）。
- 线身可以通过控制点弯曲调整。
- Agent 创建方案线时自动布局，把控间距、避免方案名和标注挤在一起；用户可在此基础上手动微调。
- 标注必须靠近所属对象，并用细引导线连接所属节点边缘或方案线中点，归属一眼可辨。

当待验证方案被确认成功时，用户或 Agent 再创建/选择目标节点，并把方案线改为绿色连接。

---

## 11. 标注需求

- 方案名称始终显示。
- 人类附加标注可以全局隐藏，也可以单独隐藏。
- 隐藏只影响画布显示，Agent 始终能够读取。
- 标注只保留一句关键信息。
- 长内容进入节点或方案关联的 Markdown 文件。
- 标注用黄色小便签包裹（人与 Agent 统一），便签黄只用于标注，不用于其它元素；不使用 emoji。

---

## 12. 属性面板需求

- 只有选择节点或方案线时才显示。
- 面板从右侧出现，不成为常驻布局。
- 只展示当前对象已经存在的字段。
- 查看状态下不把每个值包成表单输入框。
- 点击内容后进入行内编辑。
- Markdown 使用文件入口打开，不在画布上展示全文。
- 删除等低频危险操作放入更多菜单。

节点面板主要包括：名称、编号、所属路线、Markdown、标注。

方案面板主要包括：方案名称、状态、起点、目标节点、Markdown、标注。

失败和待验证方案不显示目标节点字段。

---

## 13. 菜单需求

画布菜单：

- 新建节点。
- 粘贴。

节点菜单：

- 新建问题路线。
- 添加标注。
- 打开 Markdown。
- 删除。

方案线菜单：

- 标记成功。
- 标记失败。
- 标记待验证。
- 添加标注。
- 打开 Markdown。
- 删除。

菜单不提供自由绘图软件中的颜色、线宽、填充和图层设置。

---

## 14. Agent 界面需求

### 空地图

- 中央输入区是 Agent 主入口。
- 用户可以直接描述目标并开始。
- 当前项目靠近输入区显示。

### 已有地图

- 中央输入区消失。
- Agent 入口缩小为按需打开的命令入口。
- 不显示聊天侧栏。

### Agent 工作与更新

- 工作期间只显示一行简短状态。
- 更新完成后短暂提示“地图已更新”。
- 新增或修改的对象短暂高亮。
- 不逐项弹出创建、保存和同步成功通知。
- Agent 空闲时不显示常驻状态。

### Agent 不可用

- 只说明当前无法交给 Agent。
- 提供打开设置或手动新建节点的恢复动作。
- 不在错误界面展示 Agent 供应商列表。

---

## 15. 色系方向

- 主体使用白色和轻微暖灰，保持安静、自然。
- 文字与图标使用深灰和中灰，不使用纯黑大面积压迫画布。
- 选中和工具激活参考 Excalidraw 的克制紫蓝色。
- 发送或确认操作可使用近黑色。
- 绿色、红色、灰色只用于方案状态。
- 不使用渐变和大面积强调色背景。

具体颜色值在 HTML 原型阶段根据真实浏览器效果确定。

---

## 16. 文案要求

- 使用短句和直接动作名称。
- 页面标题、按钮、菜单和标注都应尽可能短。
- 不重复表达同一状态。
- 不提前解释用户尚未遇到的问题。
- 不使用“恭喜”“太棒了”“成功创建”等情绪化反馈。

推荐：

- 想探索什么？
- 描述你想做的事
- 选择项目
- 新建节点
- 地图已更新

避免：

- 让 Agent 帮你开始一段精彩的探索之旅
- 请在此处输入项目目标，Agent 将帮助你完成
- 配置本地 Agent 执行模式与工作目录
- 从第一个目标里程碑开始

---

## 17. 原型与审核要求

- 后续使用可交互 HTML 原型审核界面。
- SVG 只用于简单结构图，不作为最终交互和视觉判断依据。
- 首先审核空地图输入和空白画布。
- 然后审核单路线、多路线和方案状态。
- 最后审核选择面板、标注和 Agent 更新状态。

每轮审核只关注：

1. 是否存在不必要的文字？
2. 是否存在不必要的控件？
3. 是否能一眼看清节点、路线和方案状态？
4. Agent 是否在需要时出现、平时消失？

---

## 18. 一句话界面基准

> 像 ChatGPT 一样自然地把目标交给 Agent，像 Excalidraw 一样安静地看清探索地图。
