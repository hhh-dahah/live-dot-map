# AGENTS.md — 活点地图 UI 原型

> 本文件面向 AI 编码 Agent，假设读者对本项目一无所知。

## 项目概述

这是「活点地图」的 **UI 原型目录**，不是完整应用。

活点地图是一个面向人机协作的探索状态画布：用少量节点、路线、方案线和标注表达项目的探索过程，详细内容保存在本地 Markdown 中，人和本地 Agent（Codex、Claude Code 等）读写同一组文件。产品形态为本地优先、免费开源、轻量无限画布（正式版计划基于 Excalidraw 改造，见 PRD §11）。

当前目录处于 PRD §16 所述阶段：**制作可交互 HTML 原型供 UI 审核**。原型通过后才会进入基于 Excalidraw 的完整功能实现。此前存在过一个 ReactFlow 验证 Demo，已废弃，不在本目录中。

## 目录结构与技术栈

无构建系统、无包管理器、无依赖——全部是零依赖的单文件静态 HTML（内联 CSS + 原生 JS），双击或任意静态服务器即可打开。

```
├── index (2).html   # 原型导航页：列出所有原型入口及审核顺序（注意文件名带空格和括号）
├── canvas.html      # 核心原型：无限画布，全部逻辑内联；打开产品直接进入画布（Excalidraw 模式，
│                    #   无独立首屏页，无中央输入态）；?blank=1 / ?empty=1 均为空白画布
├── landing.html     # 产品落地页
├── brand-spec.md    # 设计规范：色彩令牌（OKLch）、字体栈、布局姿态、文案基调
├── 设计细节.md       # 原型走查问题记录：每条含问题描述/涉及位置/正式版期望行为/状态
├── 产品需求文档-PRD.md   # PRD v2.0：产品定义、数据模型、强制规则、开发优先级
├── UI设计需求文档.md     # 界面方向、对标产品、逐条界面需求与文案要求
└── 参考ui/          # 两张 Excalidraw 界面截图（布局姿态参考，非视觉验收标准）
```

`index (2).html` 是入口：先读它了解各原型页面的用途与审核顺序。

## canvas.html 的内部结构（修改前必读）

单文件内自包含的原型实现，无框架：

- **设计令牌**：`<style>` 顶部的 `:root` CSS 变量，与 `brand-spec.md` 一一对应，改色值必须两边同步。
- **种子数据**：`seed()` 函数（约第 406 行）按 PRD §13 案例生成三条路线（动态壁纸主路线、幽灵脸问题、性能优化）的演示数据。
- **状态模型**：全局对象 `S`，含 `routes / nodes / edges / anns / view / sel / multi / tool` 等字段；节点字段 `{id,num,name,type,route,x,y,r,md}`（`num` 仅供文件命名，界面不显示；名称写在圆内，`r` 按文字自动计算），方案线字段 `{id,from,to,name,status,route,md,dx,dy,cx,cy}`（`to:null` + `dx/dy` 表示悬空末端；`cx/cy` 为弯曲控制点偏移）；`hoverEdge/snapTo/selAnn/drawingEdge` 是瞬时交互态，不进撤销快照。
- **渲染**：`render()` 全量重绘；节点是 `translate(-50%,-50%)` 定位的绝对定位 DOM（名称在圆内），方案线是 `<svg id="edges">` 内的二次贝塞尔 path，三种状态都带箭头（各自颜色的 marker）。
- **方案线交互**：悬空端点可拖拽（更新 `dx/dy`，拖动时钉住曲线前段锚点 B(0.25)，前半段基本固定、后半段自由弯折；命中节点时吸附高亮，松手自动连接变绿）；已连接（绿色）线选中或悬停时末端出现手柄，拖起即脱钩变灰（待验证），拖到别的节点松手重新吸附变绿；按住线身或中点手柄弯折（抓取点固定在指针下，按曲线参数 t 反推 `cx/cy`）；箭头工具在节点上按住直接拉出新线；方案名标签沿曲线法向偏移，同节点多条线交替取侧避让。
- **标注**：黄色小便签，归属用四角的小尾巴（小角角）连向所属对象，随拖动拉伸；自动避让布局 + 拖拽微调 + 双击复位；点击紫色选中、再点行内编辑。
- **选择**：空白处拖拽出紫色框选（`#marquee`），多选包围框 `#selbox` + 悬浮一键删除按钮 `#del-float`；平移用滚轮/中键/空格。悬浮节点/方案线时紫色发光。
- **项目菜单**：左上 `#proj-menu-btn` 弹出 Excalidraw 式菜单（打开/新建/导入地图、保存、导出），原型阶段多数为 toast 演示项，「新建地图」（`newBlankMap()`）直接切到空白画布，无中央输入态。
- **撤销/重做**：`snapshot()`/`pushHistory()` 用 JSON 快照实现。
- **方案状态切换**：`setEdgeStatus()` 强制实现连接规则（待验证→成功会自动创建目标节点）。
- **Agent 模拟**：`agentWork()`/`submitAgent()` 仅模拟 Agent 更新流程（状态提示 + 高亮），没有真实 Agent 连接。

## 构建与测试

- **无构建命令**。直接双击 HTML 或用任意静态服务器（如 `python -m http.server`）预览。
- **无自动化测试**。验证方式是按 `index (2).html` 中的审核顺序在浏览器里人工走查：空白画布与新建地图 → 单路线、多路线与方案状态 → 选择面板、标注与 Agent 更新。
- 修改 canvas.html 后至少人工检查：手掌工具平移、滚轮/中键/空格平移、滚轮缩放、拖动节点、拖动方案线悬空端点（吸附变绿、前半段固定弯折）与已连接线末端拖下变灰、线身弯折（抓取点固定）、悬浮发光、框选与一键删除、便签选中/编辑/拖拽、拉出式新建箭头、属性面板、右键菜单、左上项目菜单、三种方案状态切换、`?blank=1` 变体。

## 代码与设计规范（必须遵守）

**语言**：界面文案、代码注释、文档一律使用简体中文。

**设计令牌**（brand-spec.md）：
- 色彩一律用 `:root` 中的 OKLch CSS 变量，不引入新色值；改令牌要同步更新 `brand-spec.md`。
- 绿（`--success`）红（`--danger`）灰（`--pending`）**只能**用于方案线/状态徽标，节点和其余交互禁止使用；选中与工具激活用紫蓝 `--accent`，确认/发送用近黑 `--ink`。
- 不使用渐变、大面积强调色背景、便签色气泡、emoji。
- 浮层控件统一：8px 圆角、发丝边框、`--shadow` 轻投影、36px 图标按钮、激活态铺 `--accent-soft`。

**UI 强制规则**（UI设计需求文档.md）：
- 禁止多余文字：一个区域一个标题，无副标题、无功能解释；技术术语（CLI/MCP/skill/JSON）只出现在设置里。
- 不设全宽固定顶栏；工具条悬浮顶部中央，缩放/撤销在左下，项目入口在左上，Agent 入口缩在右下。
- 未选中对象时不显示属性面板；面板查看态不套表单框，点击行内编辑。
- 节点一律圆形，不显示编号，短名称直接写在圆内、圆随文字自动适配；方案名称附着在线上，不得做成节点。
- 方案状态几何约束：成功 = 绿色实线带箭头且必须连接目标节点；失败 = 红色虚线带箭头、末端悬空；待验证 = 灰色点线带箭头、末端悬空。失败/待验证禁止连接目标节点（PRD §5）。悬空端点可自由拖拽，线身可通过控制点弯曲。
- 标注必须靠近所属对象，用便签角落的小尾巴（小角角）表明归属，不用细引导线；拥挤问题靠自动布局 + 手动微调解决，不得挪用绿/红/灰状态色。
- 不设独立首屏页与中央输入态：打开产品直接进入画布，新建地图直接进空白画布；打开/新建/导入地图、保存/导出收进左上项目菜单（Excalidraw 式）。
- 文案用短句和直接动作名（新建节点 / 新建地图 / 地图已更新），禁止情绪化反馈（恭喜、太棒了）。

**代码风格**：保持单文件、零依赖、内联 SVG 图标（图标集中在 `I` 对象）、原生 JS、无框架。不要引入构建工具、npm 依赖或 CSS 框架。

## 安全与边界

- 本目录是纯静态原型，无网络请求、无凭据、无构建产物，无特殊安全面。
- 注意产品层面的 Agent 安全边界（PRD §9.4）：未来实现文件协议时，Agent 默认只读写当前项目目录，删除大量数据前需人确认。
- Excalidraw 为 MIT License：未来基于其代码改造时必须保留版权与许可声明（PRD §11.2）。

## 未来方向（不要在原型阶段做）

原型审核通过后才会：基于 Excalidraw 实现完整画布、定义 Agent 可读写的结构化文件规范（`MAP.html` / `nodes/` / `routes/` / `.live-dot-map/`，见 PRD §9.1）、可选本地桥接。当前阶段不要提前实现这些。
