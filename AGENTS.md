# AGENTS.md — 活点地图 · 工位协作守则

> 主工位统一维护，四工位同一份。原则一次说清；具体路径/命令以仓库与地图现状为准。

## 记忆协议
1. 进入项目优先经 `livedot-map` MCP 工具读取实时记忆与节点事实。
2. 未配置 MCP 时提示用户配置；降级仅允许只读查阅 `.live-dot-map/`，严禁写入。

## 工位模型（git worktree）
四工位 = 同一仓库的四棵工作树，各检出一条专属分支，因此多条分支**同时可改可测**（不存在改一条另一条不能动）；一个总控会话也可跨工位并行操作（完全访问 + `git -C`）。按当前目录/分支对号入座：
- **主工位** `ui设计 html`（master）：集成与发布、总控裁定与任务派发、闸门合并。
- **UI 工位** `live-dot-map-ui`（ui-exp-minimal-zen）：画布交互/视觉/禅意全屏/响应式。
- **后端工位** `live-dot-map-backend`（feat-backend-memory）：记忆引擎/协议校验/持久化。
- **适配器工位** `live-dot-map-adapter`（feat-agent-adapter）：Agent 适配/MCP 桥接/可靠性稳定性。

## 分工与派发
用户只在主工位说目标，不判断大小——裁定是主工位的职责：小而快或跨域纠缠 → 总控直做；深研循环型（需反复回读研究文档）、大体量、多任务并行 → 写任务卡到地图节点派分身（上下文隔离是工位的本体）。工位按卡执行；发现跨域或被卡住 → 上报主工位。

## 协作铁律
1. **开工先跑 `npm run start-task`**：三查不过即拒绝并说明原因——①工作区干净（有未提交 = 上单未收尾）；②无未合并进 master 的独有提交（有 = 上单待合并，**严禁手工 reset，会被丢弃**，先去主工位合并）；③对齐 master 后开工。
2. **验收即提交**：用户说"可以了"立即提交并回报「待合并」；过程小步提交；跨界改动在提交信息声明「跨界: 路径」。
3. **合并收口**：一切合并只在主工位、先过 `node scripts/merge-guard.mjs <分支>`；工位严禁自行 merge/rebase 同步或合入。
4. **产物不手工裁决**：构建产物冲突一律合源码重建（app.html 由 src/web 源经 `npm run build:app` 生成）。
5. **测试必隔离**：一律 `npm run dev`（自动隔离实例、页面带【测试】标记），不碰常驻生产桥。
