# goal.md — 重大方案与阶段路线

> 用途：记录产品的重大方案决策、阶段路线、最终形态、边界与完成标准。
> 执行过程与修改记录见 `implement.md`；走查问题细节见 `设计细节.md`。

## 产品目标

活点地图是面向人机协作的探索状态画布：用少量节点、路线、方案线和标注表达项目的探索过程，详细内容保存在本地 Markdown 中，人和本地 Agent 读写同一组文件。本地优先、免费开源、轻量无限画布。完整定义见 `产品需求文档-PRD.md`。

## 阶段路线

### 阶段 0：HTML 原型（已完成，定稿为验收样板）

- ReactFlow 验证 Demo 废弃（在 `live-dot-map/` 上级目录，不再维护）。
- 零依赖单文件 HTML 原型经 11 轮走查后用户验收基本满意，正式定稿（提交 `c68969f`）。
- 验收样板即本目录的 `canvas.html`：入口、节点、路线、状态、标注、面板应该长什么样、怎么操作，以它为准；原型不再修改，遗留小项见 `implement.md`，留待 Excalidraw 版对照。

### 阶段 1：文档与 Excalidraw 环境（已完成）

- git 提交冻结原型；建立 `goal.md` / `implement.md`；精简 `AGENTS.md`。
- 克隆 Excalidraw 源码到 `D:/桌面/活点地图/excalidraw`，本地安装并跑通（`yarn start`，本机端口 3001）。
- 不改 Excalidraw 任何代码。

### 阶段 2：画布可行性验证（当前阶段）

正式修改fork下来的Excalidraw代码 这一阶段目标是利用excalidraw的画布引擎 实现 我的canvas.html的所有画布功能的 至少是要包含我html上展示的所有画布功能的 操作逻辑 设计细节可以看 设计细节.md 有很多细致的逻辑


### 阶段 3：正式功能

可行性验证通过后，按此顺序推进（对应 PRD §15 P0 → P1）：

1. 地图文件格式（`MAP.html` / `nodes/` / `routes/` / `.live-dot-map/`，见 PRD §9.1）；
2. Markdown 关联；
3. 手动编辑完善；
4. Agent 读写；
5. 实时同步。

## 最终形态（要改成什么样子）

基于 Excalidraw 改造的本地优先单页应用：

- 打开即画布，无独立首屏页、无中央输入态（Excalidraw 模式）。
- 圆形节点：名称写在圆内、圆随文字自动适配、不显示编号。
- 三色方案线，均带箭头：绿色实线 = 成功；红色虚线 = 失败；灰色点线 = 待验证。状态与连接解耦：三态均可连接或悬空，改状态不动连接、改连接不动状态（走查 10）。悬空端点可拖拽、命中节点吸附连接；线身可弯折调整。
- 方案名附着在线上；标注为黄色小便签、用小尾巴表明归属，自动避让 + 手动微调。
- 选中对象显示简洁属性面板，行内编辑；节点/方案关联本地 Markdown 详情。
- 数据即文件：Agent 与人读写同一组结构化文件（PRD §9.1）。
- Excalidraw 多余的绘画工具隐藏或收进更多菜单；保留其画布、缩放平移、箭头绑定、撤销重做等基础能力。

## 边界（不做什么）

- 沿用 PRD §12 非目标：不做通用白板、富文本笔记、Jira 式项目管理、产品内聊天客户端、云端模型市场、强制登录与云同步、多人实时光标协作、移动端完整编辑。
- 可行性验证阶段（阶段 2）额外边界：数据写死固定示例；不做文件读写、不接真实 Agent、不做导入导出。
- 不顺手重构 Excalidraw 内部架构，不升级其依赖版本，不换技术栈。
- Excalidraw 为 MIT License：复用或修改其代码必须保留版权与许可声明，并使用活点地图自己的产品名称和品牌（PRD §11.2）。

## 完成标准（打算完成到什么地步）

- **阶段 0（已达成）**：原型经 11 轮走查定稿并提交，冻结为验收样板。
- **阶段 1（已达成）**：原型已冻结并提交；`goal.md` / `implement.md` 建立、`AGENTS.md` 精简；Excalidraw 在本机 `yarn start` 跑通可访问。
- **阶段 2（当前验收目标）**：上述 7 项验证在 Excalidraw 上逐项通过，暴露的问题有结论。
- **阶段 3**：PRD §15 P0 清单逐项落地，达到 PRD §14 成功标准。
