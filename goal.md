# goal.md — 重大方案与阶段路线

> 用途：记录产品的重大方案决策、阶段路线、最终形态、边界与完成标准。
> 执行过程与修改记录见 `implement.md`；走查问题细节见 `设计细节.md`。

## 产品目标

活点地图是面向人机协作的探索状态画布：用少量节点、路线、方案线和标注表达项目的探索过程，详细内容保存在本地 Markdown 中，人和本地 Agent 读写同一组文件。本地优先、免费开源、轻量无限画布。完整定义见 `产品需求文档-PRD.md`。

## 阶段路线

### 阶段 0：HTML 原型（已完成，定稿为验收样板）

- ReactFlow 验证 Demo 废弃（在 `live-dot-map/` 上级目录，不再维护）。
- 零依赖单文件 HTML 原型经 11 轮走查后用户验收基本满意，正式定稿（提交 `c68969f`）。
- 验收样板即本目录的 `canvas.html`：入口、节点、路线、状态、标注、面板应该长什么样、怎么操作，以它为准；原型保持冻结，正式产品从其复制出的 `app.html` 演进。

### 阶段 1：文档与 Excalidraw 环境（已完成）

- git 提交冻结原型；建立 `goal.md` / `implement.md`；精简 `AGENTS.md`。
- 克隆 Excalidraw 源码到 `D:/桌面/活点地图/excalidraw`，本地安装并跑通（`yarn start`，本机端口 3001）；阶段 1 内不改其代码。

### 阶段 2：Excalidraw 画布复刻（已完成，路线否决并存档）

- 在 fork 上复刻了 canvas.html 的全部画布功能（T1–T22：品牌主题/工具栏/菜单/属性面板/圆形节点/三态方案线/便签避让/路线升级/删除级联/示例场景），随后经走查修复 W1–W14，交互复刻基本完整，TS 0 错误。
- **用户决策（2026-08-06）：否决 fork 路线。** 底层要做人机共享记忆的自定义数据格式与交互（map.json + Markdown 分片），Excalidraw 的数据模型与架构由助力变成约束；组件复用的收益不抵改造成本。
- 存档：master（干净基线）已推送至 GitHub 私有仓库 `hhh-dahah/live-dot-map-canvas`；全部改造工作保留在本地 `wip-stage2` 分支，仅作交互参考实现，不再投入。

### 阶段 3–4：协议、正式版与记忆生命周期（核心实现已完成）

- `app.html` 已完成 map.json 读写、项目文件夹直连与轮询同步；数据协议升至 v1.1，加入评分、归档与投影读取。
- 壁纸项目已生成迁移地图并用于狗粮验证；文件夹直连等必须由用户授权的最终手测并入当前真实用户实测。方案与验证见 `docs/plans/2026-08-06-阶段3-协议层与HTML正式化.md`、`docs/plans/2026-08-07-记忆系统演化路径研究.md` 和 `implement.md`。

### 阶段 5–6.5：分发、接入与国内直连（已完成）

- 已完成首次引导、演示地图、agent-kit、PWA、一句口令接入与三源分发。
- `livedotmap.top` 已上线并通过国内关代理实测；部署与验收细节只保留在 `implement.md`。

### 当前：v2 可靠性候选验收

- v2 的可靠存储、本地桥、人类标注闭环、确定性图检索、画布强模式与三种 Agent 适配已实现；自动化证据见 `implement.md`。
- 里程碑状态由来源决定语义：Agent 可以把自身结论写成 `approved`，但不得伪造 `human_created`；画布必须显式展示创建者、更新者、来源和证据。
- 当前 Codex、Kimi Code 的真实 CLI 闭环已有临时项目证据；Claude Code 仍需完成真实会话闭环，普通小白仍需完成首次地图可读性与全流程验收。Node SEA、WinForms Setup、安装失败回滚、卸载恢复已有本地证据；干净机/升级人工验收、Store/Release 发布仍是公开 RC 门禁。在此之前不宣称三种 Agent 已正式验收，也不标记“可靠性实现完成”。
- 分发准备与开发/自动测试并行：SEA、MSIX、Partner Center 和线上发布不阻塞本地桥、画布、协议开发；计划与门禁见 `docs/plans/8-12上线plan.md`。
- 通过后继续按 `docs/真实用户实测记录.md` 做新用户实测和对抗性审查；重大方向写本文件，执行与证据追加到 `implement.md`。

## 最终形态（要改成什么样子）

自研单文件画布 + 轻量本地桥的本地优先应用：

- 打开即画布，无独立首屏页、无中央输入态（Excalidraw 模式）。
- 圆形节点：名称写在圆内、圆随文字自动适配、不显示编号。
- 三色方案线，均带箭头：绿色实线 = 成功；红色虚线 = 失败；灰色点线 = 待验证。状态与连接解耦：三态均可连接或悬空，改状态不动连接、改连接不动状态（走查 10）。悬空端点可拖拽、命中节点吸附连接；线身可弯折调整。
- 方案名附着在线上；标注为黄色小便签、用小尾巴表明归属，自动避让 + 手动微调。
- 选中对象显示简洁属性面板，行内编辑；节点/方案关联本地 Markdown 详情。
- 数据即文件：map.json 索引 + Markdown 分片，Agent 与人读写同一组文件（PRD §9.1）。
- 正式协同由本地桥统一处理 WAL、原子保存、冲突、恢复、检索与 Agent 写回；浏览器只负责画布交互。
- `app.html` 仍可双击使用，但纯浏览器模式只作明显标记的降级模式，不承诺 Agent 自动读取与并发保护。

## 长期研究方向：仿生记忆

- 仿生记忆是产品长期核心路线，不是营销比喻；详细研究基线见 `docs/plans/2026-08-07-记忆系统演化路径研究.md`。
- 第一版先把共享记忆做成可解释、可审阅的策展图：控制节点数量，压缩过程细节，突出目标、问题、决策、证据和人类新信息，不宣称已经模拟完整人脑。
- 后续推进“睡眠整理”：周期性提出合并、摘要、归档、路线重组和记忆强化建议，经人确认后执行，历史证据不得丢失。
- 长期研究跨路线联想、关联激活、遗忘与强化、专家式全局判断，并建立可重复评测；算法允许演进，但人机共享、对人可见、判断权归人的产品边界不变。

## 边界（不做什么）

- 沿用 PRD §12 非目标：不做通用白板、富文本笔记、Jira 式项目管理、产品内聊天客户端、云端模型市场、强制登录与云同步、多人实时光标协作、移动端完整编辑。
- 交付仍是单个 `app.html` 与无运行时 npm 依赖的 `livedot.mjs`；内部源码可用模块化 TypeScript。
- 不上传项目内容、不做遥测、不引入云数据库或向量 embedding。
- 存档的 Excalidraw fork 仅作参考；如未来复制其代码片段，须遵守 MIT License 保留版权与许可声明（PRD §11.2）。

## 完成标准（打算完成到什么地步）

- **阶段 0（已达成）**：原型经 11 轮走查定稿并提交，冻结为验收样板。
- **阶段 1（已达成）**：原型已冻结并提交；`goal.md` / `implement.md` 建立、`AGENTS.md` 精简；Excalidraw 在本机 `yarn start` 跑通可访问。
- **阶段 2（已关闭）**：fork 复刻完成并走查修复后，用户决策否决该路线；工作已存档，结论记录如上。
- **阶段 3–4（核心实现已达成）**：协议层、`app.html` 双向读写和记忆生命周期协议已落地；需要用户授权手势的真实项目验收并入当前阶段。
- **阶段 5–6.5（已达成）**：分发、自动接入、三源兜底与国内直连已完成并验收。
- **v2 当前验收目标**：三种 Agent 各完成一次真实 SessionStart → 引用人类标注 ID → ack → 写回 → Stop 闭环；随后进入新用户实测与对抗性审查。
