# 流程图错位+标题

图片不能裁切

流程图全部审核看看有没有表达错误

你这个流程图是不是还是有问题呀 咋回事流程图画不好？ 画完之后必须渲染检查不然不通过 这种我一眼看出来逻辑不通关的问题不能出现

改动包括：
- 图 1：四层各自对应一条访问规则，不再错误汇入同一位置；去除重复标签。
- 图 2：补上“是否为人类经应用保存”的是/否分支。
- 图 3、4：补上新鲜度/元数据判断分支，并修正图 3 的汇合文字重叠。
- 图 5：补上“未确认则持续呈现”的回环。
- 摘要附图：正文与资产的显式下探分别标明。
- 标题结构保持模板规则：只有说明书首页出现一次发明名称。
## 本轮实施结果（2026-08-24）

### 修正内容

- 图 1：四层结构分别连接到“结构投影、摘要检索、正文：显式路径读取、资产：显式路径读取”，不再把四条线错误汇入同一个规则框；移除重复的“显式路径读取”。
- 图 2：保存结果由“是否为人类经应用保存？”的菱形判断分出“是/否”两条路径；两支先汇入明确的黑色汇合点，再输出“返回保存结果和版本标记”。
- 图 3：补上新鲜度判断；复用摘要和读取/刷新正文两支经明确汇合点后，再统一进入排序、附召回理由和结果输出。
- 图 4：补上元数据一致性判断；复用摘要和重读正文/刷新单卡两支经明确汇合点后，再继续检索输出。
- 图 5：保留并核对“未确认则持续呈现”的回环。
- 摘要附图：把正文和资产两类下探分别标明，避免抽象重复标签。
- 图 6—8：保留产品界面图且不裁切；仅作为实施例辅助图，不承担性能证明。
- 标题：摘要、权利要求书、说明书各节只保留模板要求的页眉；发明名称仅出现在说明书首页一次，未在分节首页重复。

### 渲染验收

前一版虽能编译，但渲染后存在“分支关系不直观、线条可能让人误读为进入文字框”的问题，已撤回，不作为验收结果。

- 已以 XeLaTeX 连续编译两次，生成 16 页 A4 PDF；无编译错误、无未定义命令或引用。
- 已逐页渲染核对摘要附图与说明书图 1—5。图 2、3、4 的每个判断框均有明确的两条出边、箭头方向、分支文字和独立汇合点；连接线不压住框内文字，也不再以侧向进入结果文字框。
- 复跑索引、人类更新、项目存储自动化测试：26 通过、0 失败。
- 当前交付文件为 `docs/patent-tex/main.pdf`。

### 本地桥恢复

- 已停止误指向旧安装测试目录的桥，仅重启当前项目的桥。
- 已通过项目自身的原子记录方式更新“当前项目”指针；复核桥进程正在监听，且地图服务已回到本专利地图（`map-mt5bdmpb`）。
- 后续流程图修改固定执行“修改 → 编译 → 逐页渲染检查 → 覆盖主 PDF”四步；仅编译通过不视为验收通过。