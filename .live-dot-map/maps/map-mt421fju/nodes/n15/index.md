# 新节点16<mark>

---

## 修 Bug 排序计划（agent 整理 2026-08-29，按依赖关系排）

已解决：n1（切换项目 agent 不知道）、n3（owner 参数被拒）。e2e-node 已归档。
待处理：n4、n5、n6、n7、n8、n9、n10、n11、n12、n13、n14。

### P0 — 先修（小、独立、每天测试都在踩）

1. **n9 误定位：检索不到节点名字**
   - 卡片索引层（md-index）已建好，修检索权重/节点名匹配即可。工作量小，无依赖。
2. **n7 打开文件夹不能直达文件 + 无快捷保存 + 未保存却亮冲突灯**
   - 三个小修打包：打开方式直达文件路径、Ctrl+S / Ctrl+Enter 保存、状态灯把「未保存」和「冲突」分开。无依赖。
3. **n12 agent 擅自改人类原话 → 追加式写入**
   - 工具层加约束 + 协议文档写明「人的原文只追加不覆盖」。这是人机信任根基，且后面 n6、n14 都建立在写入契约上。

### P1 — 编辑器体验（n5 依赖 n8 的决策，必须一起定）

4. **n8 内置编辑器图片问题 → 先做决策**：要么支持图片粘贴自动入库，要么明确不支持并引导外部编辑器。这个决策直接决定 n5 怎么改。
5. **n5 UI 编辑器改侧边栏（模仿 codex 侧栏，不遮挡地图）**
   - 大前端改造，在 n8 定了编辑器定位之后做，避免返工。n7 的冲突灯修正若与状态栏重叠，顺手一起。

### P2 — 人机感知与并发（架构级，协议稳定后做）

6. **n6 人的编辑 agent 主动感知（不强制 MCP）**
   - 桥已有文件监听/卡片信号，方案是把 humanUpdates 通过 hook 主动注入上下文。与 n12 同属人机协作协议，建议同一批设计。
7. **n10 并发：多项目多 agent 时桥指错地图**
   - 依赖已修的 current-project 指针，但要扩到多开场景：显式管理界面或让 agent 可指定项目+地图。改完需真实多开验收。

### P3 — 新功能（等 P2 协议稳定）

8. **n4 跨地图外部指针**：协议扩展，独立，放后面。
9. **n14 agent 自主创建节点写细节**：MCP 已能建节点，缺的是触发策略；必须等 n6/n12 的人机读写契约稳定后再定，否则 agent 自主写的内容又会踩 n12 的问题。

### P4 — 非代码事项

10. **n11 产品定位**、**n13 微软商店上线日程**：不占开发批次，随时可讨论。

### 关键依赖链（一句话版）

- n12（追加式写入）→ n6（主动感知）→ n14（agent 自主写）
- n8（图片决策）→ n5（编辑器 UI 重构）
- 指针机制（已修）→ n10（并发管理）
- n9、n7 完全独立，随时可插队和验收
---
</mark>
